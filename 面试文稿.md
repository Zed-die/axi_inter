















面试官好，我叫王昊宇，本科和硕士均就读于西安电子科技大学通信工程学院，本科期间综合成绩 95.91，排名前 7%，GPA 3.8，并获得研究生保送资格；硕士阶段就读于空天地一体化综合业务网国家重点实验室网络与交换团队，GPA 3.8，多次获得奖学金校级优秀学生等荣誉。

我的主要研究和项目经历集中在高速接口、片上总线方向。在团队项目中，我深度参与了 TSN/TTE 相关开发，并完全负责 IEEE 802.1AS 协议的硬件卸载以及测试仪设计，其中包括可复用的 PCIe DMA 引擎。同时，我也设计并实现了一个基于 AXI 的 ICN，包括 AXI Crossbar、APB 配置接口，AXI2AHB 桥接，并实现了一个 CHI RN-I 节点，支持 AXI 读写请求与 CHI flit 之间的相互转换，同时我还完成了中兴通讯领军班的微型结课小课题，《NVME OVER TCP 硬卸载关键技术研究》。

此外，我还在华为海思图灵业务部参与了数字 IC 方向的实习，独立完成了虚拟项目的全流程开发，包括 AS 撰写与方案评审、RTL 设计与验证，以及最终的实现与 PPA 优化。通过这段经历，我对芯片研发的规范流程有了更加深入的理解，也进一步提升了从系统需求出发进行微架构设计、功能验证和 PPA 优化的工程能力。

科研和竞赛方面，我在研究生期间共参与发表 7 篇国家发明专利，并完成了一篇小论文；同时获得过嵌入式芯片与系统设计竞赛国家二等奖、研究生智慧城市技术与创意大赛国家三等奖、数学建模省级一等奖等多项奖项。

除了技术学习和项目实践，我也比较重视知识总结与分享，独立运营了微信公众号“数字 ic 前端设计”，目前已收获 4000 多位粉丝关注和超 7 万次阅读量。

以上就是我的自我介绍，谢谢！



# 实习经历

我做的这个项目是一颗基于 Verilog 实现的 32 位 RISC‑V 处理器核，整体采用经典的 IF、ID、EX、MEM、WB 五级流水线架构，属于单发射、顺序执行、顺序提交的设计。在模块划分上，我把处理器拆成了取指模块 IFU、译码模块 IDU、执行模块 EXU、访存模块 MEMU、写回模块 WBU，以及寄存器堆、指令存储器、数据存储器和统一控制模块。这样划分的好处是数据通路和控制通路比较清楚，各流水级之间通过寄存器传递 PC、操作数、目的寄存器地址以及 valid、写使能等控制信号。

当前版本实现了 RV32I 中主要的 37 条整数运算、访存和控制流指令，包括算术逻辑、立即数运算、Load/Store、条件分支、JAL 和 JALR，另外实现了 WFI 指令。

这个项目中我重点处理了两类流水线冒险。第一类是数据冒险：在译码阶段加入了 forwarding，可以从 EX、MEM 和 WB 阶段把结果旁路回源操作数，减少不必要的停顿。对于 Load 指令，因为使用的是同步数据存储器，数据到写回阶段才稳定，所以遇到紧随其后的相关指令时，会冻结 IF 和 ID，并向 EX 插入气泡，等数据能够从 WB 阶段旁路后再继续执行。

第二类是控制冒险。处理器采用静态“始终不跳转”预测，正常情况下 PC 每拍加 4；分支和跳转在 EX 阶段完成判断。如果发现需要跳转，EXU 会产生 redirect PC，控制模块冲刷流水线中的 IF 和 ID 两级，因此一次跳转大约损失两个周期。

访存方面，数据存储器采用 256×128 bit 的组织方式，通过 128 bit 写掩码实现字节、半字和字写入，并在写回阶段根据地址低位完成 LB、LH、LW、LBU、LHU 的数据选择和符号扩展。

除了基本数据通路，我还实现了非法指令和 PC 非四字节对齐异常。异常会沿流水线传递到 MEM 阶段再统一上报，并记录对应的异常 PC；同时支持通过配置位屏蔽异常输出。外部异步的启动脉冲则通过多级寄存器同步和边沿检测进入处理器时钟域。

在设计完成之后，基于现有的UVM环境分别进行了冒烟测试，回归测试以及门级仿真，测试通过后进行DC综合，通过分析关键路径，只复位控制通路，分析时钟门控率等方式优化处理器PPA。











# 项目经历

TSN网络是一个时间敏感网络，要求关键报文不仅能够正确到达，还要在规定时间内到达，具有较小的时延抖动和明确的时延上界，如果网络中的各个端节点和交换机的时间不一致，上一级交换机的发送窗口和下一级交换机的转发窗口可能无法正确衔接，报文可能需要等待下一个调度周期，甚至被时间门禁过滤。因此，802.1AS在TSN网络中必不可少，他为TSN网络中的所有节点提供统一的时间基准，是时间感知调度机制正常运行的基础。

传统 Linux ptp4l 等方案容易受系统调度、协议栈开销影响，引入不可控时间抖动，同时增加CPU 负担，因此我采用了全硬件设计，基于协议标准设计了端节点以及网桥的IP，同时在研究所的需求下，基于协议测试标准设计并实现了IEEE802.1AS同步测试仪，代码总数。

第一步是选主：网络中的各个节点会通过 BMCA 算法比较时钟优先级、时钟精度、时钟质量等信息，最终选出一个最优的主时钟，也就是 Grand Master。其他节点都以它作为全局时间基准。

第二步是测延迟：gPTP 通过携带时间信息的报文交互，能够计算出相邻两个节点之间的链路延迟，也就是 PPT 中间部分的 P2P 延迟对等算法。

第三步是校时钟：主时钟会周期性发送 Sync 报文，并通过 Follow_Up 携带更精确的发送时间戳。从节点收到后，结合前面测得的链路延迟，计算自己和主时钟之间的时间偏差 offset，然后修正本地时钟，使本地时间逐步对齐到主时钟。



# IEEE 802.1AS 终端架构

左边是与PHY层的交互接口，主要负责gptp帧的接收或发送的时间戳脉冲生成以及报文的跨时钟域处理，本地时钟域下产生的时间戳脉冲会被跨越到系统时钟域下,驱动系统侧采样当前 timer，这样得到的时间戳对应的是报文最靠近物理接口的到达时刻。

报文跨时钟域后进入帧解析模块，对于不同类型的报文提取相应的帧字段并进行帧校验，Announce 报文中的时钟属性信息被送到 BMCA 模块，本地时钟属性信息通过clock config 配置,通过优先级向量比较决定本端口是主时钟还是从时钟；Pdelay、Presp和 Sync 相关时间信息送到 compute 模块，用 T1/T2/T3/T4 计算链路延迟，再结合 Sync 帧的接收时间点计算本地时钟偏差 offset。

发送方向上，时间管理模块根据 BMCA 输出的端口状态和配置的帧间隔产生各个gptp帧的发送使能，同时维护本地 timer 和经过校正的 sys_time，并输出设备同步状态和 1PPS 脉冲。frame_gen_OC 根据这些使能生成对应的 gPTP 帧，并在模块内部维护SequenceID等帧字段，考虑到帧生成脉冲的并发情况,对于各个帧生成之前添加了一个小buffer，经过 SP 仲裁、位宽转换和 CRC 添加后输出给PHY层的交互模块。



# IEEE 802.1AS 网桥架构

网桥架构与终端的大部分模块均可以直接复用，核心区别在于 BMCA 选举和端口状态管理模块。

为了适配多端口的 BMCA，网桥会对各端口收到的 Announce 时钟属性信息进行缓存，并通过老化计数判断该候选时钟信息是否有效,当收到新的 Announce报文或候选状态更新时(出现了超时失效)，模块会发起一次新的比较,模块会在比较流水线启动前对当前各个端口的时钟属性信息做一次快照，保证本轮的各个端口的时钟属性使用的是同一时刻的数据，之后再基于这组快照数据进行分级流水比较，最终选出全局最佳主时钟。这样既避免了多端口信息在比较过程中变化造成结果不一致，也降低了一次性大位宽组合比较带来的时序压力。

网桥由端口状态管理模块统一管理各端口状态，它根据 BMCA 输出的最佳端口来判定哪个唯一端口为从端口，其余全部置为主端口，或者网桥本身为主时钟，将所有端口均置为主端口。

被选为 Slave 的端口会把计算出的 offset 送入时间管理单元，用于校正全局 sys_time，其余 Master 端口则基于这套已经校正后的系统时间向下游发布新的gptp报文.



# PCIe DMA架构

接下来介绍测试仪架构，首先介绍测试仪与主机侧交互的 DMA 引擎架构设计，这是一个基于 Xilinx PCIe 硬核 IP 的 SG DMA 引擎，接口侧数据位宽是 128bit(其中若PCIe配置为 Lane4则对应的主频为125Mhz, Lane8 则对应的主频为250Mhz)。

接收方向上，接收引擎会把接收 TLP 根据 Type 字段类型分成两类：一类是请求类报文，也就是主机发来的Mwr与Mrd报文，主要用于 BAR 空间寄存器访问；另一类是完成类报文，也就是板卡侧主动发起 MRd 后主机返回的 Cpld数据。请求类报文被解析后会进入 BAR 寄存器空间读写模块，根据地址偏移来映射各个寄存器，实现寄存器的配置和读取，完成类报文被解析后进入乱序处理模块，由于发送的 Mrd 的 AT 字段置为 relaxed ordering，所以 Cpld 可能按照 tag 乱序返回，当前的设计支持参数化Tag的数量，这里分了内部 Tag 与外部 Tag，外部 Tag 即是发送的Mrd报文携带的Tag号，内部Tag用于标识数据类型以及通道号，外部Tag与内部Tag一一对应,这里会有一个RAM专门用于管理每个内部Tag对应的外部Tag编号，返回的数据根据其 Tag 放入 RAM 对应的地址空间中，再按照由小到大顺序输出，并根据外部Tag映射得到的内部Tag将数据分发给对应方向，如果有先完成的Tag，会被阻塞在RAM中直到他前面的Tag的数据也接收完毕才会输出。

DMA引擎支持参数化操作，通过 generate 例化多个 dma_channel，每个通道内部主要分成 rx_port 和 tx_port 两条路径。

rx_port 负责描述符获取以及DMA数据获取，主机在传输开始前会先通过PIO操作告知板卡此次DMA下发的长度，偏移等信息，再告诉描述符在内存中的地址以及长度信息，描述符请求模块根据MRRS，4K边界限制拆分描述符获取请求，获取到的描述符会被批量存储到buffer中，DMA下发控制模块从buffer中读取描述符，根据描述符长度，MRRS以及4K边界限制切分DMA数据请求，收到的 cpld 报文经过乱序处理、重新打包，然后通过异步fifo跨时钟后发送给用户侧接口。

tx_port 负责DMA数据的上报，先在用户接口处给出本次上报传输的长度以及偏移信息，引擎完成上报信息握手后开始接收上报数据，上报数据经过跨时钟域接收并统计用户数据，tx_port_writer 根据 TX SG 描述符生成 Memory Write 请求，数据从 tx_port_buffer 送出。

多个通道产生的读请求和写请求会统一进入 tx_multiplexer，这里会做通道仲裁、tag 分配、读写请求选择，并最终交给 txr_engine 组装成 PCIe MRd 或 MWr TLP。寄存器读返回的 Completion 则由 txc_engine 生成，tx_mux 再在 TXR 请求通路和 TXC completion 通路之间做加权仲裁，统一送回 PCIe hard IP。

PCIe 硬核 IP 通过 FC 接口输出当前可用的头部信用和数据信用，按照最差情况估计，使用MRRS除于RCB得到需要的最大包数，计算出需要的报头信用值，并通过MRRS 计算出需要的数据信用值，每4DW消耗一个数据credit，两者信用值均满足时，才允许发送Mrd请求。

中断部分由 interrupt 模块完成，每个通道有五类事件：TX 新事务、TX SG 接收完成、TX DMA 完成、RX SG 接收完成、RX DMA 完成事件会被打包到两个 32bit interrupt vector 里，任意中断触发后会触发驱动向下读取中断寄存器来判断是哪种中断事件，同时消除中断标识位。

reorder_input 的五级流水线主要是为了在 128-bit 数据宽度下实现每拍一个 Completion beat 的吞吐。因为每个 Tag 需要维护累计 DW 数量和四个 RAM bank 的写指针，而且元数据 RAM 是同步读，所以我把处理拆成五级：先读 count，再计算对齐偏移；然后读 position，更新四路 bank 指针；最后计算物理地址并旋转 Payload。对于同 Tag 连续返回的场景，我加入了 count 和 position 的数据前递，避免 RAM 写回延迟造成 RAW hazard。五级不是固定答案，而是时序、资源和吞吐之间的折中。如果目标频率变化，还可以重新划分级数并调整旁路范围。

五级流水线：

1. 根据 Tag 读取该 Tag 已累计的 DW 数，并检测连续同 Tag 的数据冲突。
2. 累加本拍有效 DW 数，计算数据需要移动的对齐偏移。
3. 读取该 Tag 在四个 RAM Bank 中的写指针，并旋转写使能。
4. 更新各 Bank 的写指针，计算 Tag 基地址和数据移位量。
5. 生成最终 RAM 地址，对齐数据并写入，同时产生长度、完成和错误信息。

五级不是 PCIe 协议规定的，而是根据数据依赖和时序划分的。这里有“计数 RAM 读取—计数更新—位置 RAM 读取—位置更新—数据对齐写入”这条依赖链，而且 RAM 读取、加法、旁路选择和桶形移位组合路径较长。拆成五级可以缩短关键路径、满足工作频率，并在流水线填满后做到每拍处理一个 128 bit 数据。级数更少不利于时序收敛，更多则只会增加延迟、寄存器和旁路复杂度，所以五级是频率、吞吐和资源之间的折中。





# IEEE 802.1AS同步测试仪架构

同步测试仪的主体架构如图所示，上位机通过 PIO 下发测试配置，配置内容包括端口模式、时钟参数、测试时长以及每个端口的测试表项等；根据BAR偏移地址将数据分流到各个端口的独有的配置寄存器和错帧配置表，所以系统支持四端口并行测试。

每个端口内部我设计成“双数据面”结构：一个是协议一致性测试平面，另一个是故障注入平面。协议一致性测试平面除了包含前面提到的终端IP，还支持同步寄存器数据上报以及通过LSFR伪随机数生成模块实现的百分比随机丢帧功能。

测试通路则由表项驱动，表项解析模块读取fifo中的配置数据并按照格式进行解析，得到发帧的时间点，发帧数，帧类型以及错误码等信息，交给测试帧生成模块进行错帧的产生，同时启动 monitior 模块进行监管，判断DUT是否对错帧进行响应。

我还在靠近物理层的地方做了探针故障注入设计，可以通过配置端口工作模式让端口 0/1、端口 2/3 实现透明转发，然后将测试板卡以探针的形式接入进网络中，在转发过程中通过探针修改模块对 gPTP 指定字段做在线篡改，比如 MAC地址，帧类型等，切换逻辑只在帧间空闲窗口生效，避免中途切模式造成断帧。

我在测试仪里设计了一条“完整帧缓存”通路，主要用于把四个 SFP 端口上的收发报文根据端口分区域缓存到 DDR3中，再通过 PCIe DMA 上传给上位机。端口侧由帧缓存模块负责抓取 TX/RX GMII 数据流，检测帧起始，统计帧长和本地时间戳，并把 8bit GMII 数据打包成 64bit 数据。每帧前面会加一个DMA报头，0xBEEF + length + port_num，方便上层解析。

使用LSFR实现 10%随机丢帧：

```
reg [15:0] lfsr;
wire       random_drop_hit;

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        lfsr <= 16'hACE1; // 赋非零初值
    end else begin
        // 16位 Galois LFSR (抽头: 16, 14, 13, 11)
        lfsr <= {lfsr[14:0], lfsr[15] ^ lfsr[13] ^ lfsr[12] ^ lfsr[10]};
    end
end

// 10% 概率阈值: 65536 * 10% ≈ 6553
assign random_drop_hit = random_drop_en & (lfsr < 16'd6553); 
```

注意复位值不能够为0，要不然会导致数值一直为0。

<img src="C:\Users\wanghaoyu\AppData\Roaming\Typora\typora-user-images\image-20260422204956404.png" alt="image-20260422204956404" style="zoom: 80%;" />

要想实现万兆：需要通过 `xgmii_analysis_rx` 和 `xgmii_analysis_tx` 两个模块识别收发方向的 gPTP 数据帧，并产生时间戳触发脉冲。由于 10G XGMII 接口每个时钟并行传输 8 字节，模块会逐个扫描 8 个 lane，通过控制字符 `0xFB` 和 `0xFD` 判断帧的开始与结束，同时统计帧内有效字节。根据以太网帧格式，在固定位置检查 EtherType 是否为 `0x88F7`，然后读取 PTP 报文的 `messageType`，区分 Sync、Pdelay Request、Pdelay Response、FollowUp 和 Announce 等报文，并为对应类型产生一个时钟周期的有效脉冲。该设计支持 XGMII 帧从 lane0 或 lane4 开始的情况，生成的脉冲再通过 `xpm_cdc_pulse` 从 156.25 MHz 的 XGMII 时钟域安全同步到 125 MHz 的业务时钟域，供下游锁存系统时间或触发响应报文。整体上，它采用了“帧起始检测、并行字节计数、协议字段识别、报文类型译码、脉冲跨时钟域”的设计思路。



# AXI ICN的设计与实现

在ICN中，最关键的是中间的AXI-Crossbar，它支持参数化上下游端口数，支持读写连接矩阵、secure 属性、各端口 outstanding和不同ID的乱序返回。

这是一个参数化的 N×M AXI crossbar，整体分为独立的读 crossbar 和写 crossbar，因此读写请求可以并行完成译码、仲裁和数据返回。它的核心设计可以概括为四部分：地址译码与准入控制、输出端口仲裁、基于扩展 ID 的响应路由，以及面向时序的寄存切片。在时序方面，我把寄存切片主要放在组合路由之后。前向 AW、W、AR 在 S 侧默认 bypass，在 M 侧分别使用 simple、skid、simple buffer；返回 B、R 在 M 侧 bypass，在 S 侧分别使用 simple 和 skid buffer。W、R 属于连续数据通道，因此使用无气泡的 skid buffer；AW、AR 和 B 的带宽压力相对较低，使用面积较小的 simple buffer。对于写通路，每个输入端口首先通过 `axi_crossbar_addr` 完成地址、连接权限和安全属性译码，并得到目标输出端口和 Region。模块内部使用 thread 表跟踪活跃 ID。每个 thread 记录 ID、目的端口、Region 和该 ID 的在途事务数量。同一 ID 的新请求只有在目的端口和 Region 与已有记录一致时才能继续，否则会被阻塞，从而保证同 ID 事务的返回顺序。并发控制分为三个维度：`S_THREADS` 限制输入端口活跃的不同 ID 数量，`S_ACCEPT` 限制输入端口的总在途事务数，`M_ISSUE` 限制每个输出端口的总在途事务数。合法 AW 译码后，会向目标 M 端口发起轮询仲裁。仲裁选择的源输入端口会被锁定给 W 通道，直到对应 burst 的 `WLAST` 握手。因为 AXI4 的 W 通道没有 WID，所以必须通过 AW 建立并保持整个 W burst 的路由关系。AWID 发往下游时，在高位加入源输入端口索引。下游返回 B 时，根据 BID 高位恢复源输入端口，低位 ID 返回给上游。每个输入端口会在所有下游 B 响应与本地 DECERR 之间进行仲裁。如果 AW 地址译码失败，crossbar 不向任何下游端口发送 AW/W，而是拉高 WREADY 吸收完整写数据 burst，等 `WLAST` 完成后生成一拍 `BRESP=DECERR`。这样不会提前返回写响应。读通路与写通路的地址译码、AR 仲裁和 ID 扩展基本一致，但没有 W 路由锁定。R 返回时根据 RID 高位路由到源输入端口，并在最后一拍 `RLAST` 完成时释放事务计数。对于读 DECERR，crossbar 根据 `ARLEN` 本地产生 `ARLEN+1` 拍零数据，所有拍的 `RRESP` 都为 DECERR，最后一拍拉高 RLAST。



在这个 ICN 项目中我还设计了 APB 配置接口和 AXI-to-AHB 桥接模块。

APB 从设备接口。一次访问分为 setup 和 access 两个阶段：setup 阶段由主机拉高 `PSEL` 并给出地址、读写方向和写数据；下一拍拉高 `PENABLE` 进入 access 阶段，模块通过 `PSEL && PENABLE` 判断有效访问，本地的pready持续拉高。当前寄存器访问不需要插入等待周期，因此 `PREADY` 始终拉高。写操作根据 `PWRITE` 和地址译码，在握手时更新对应配置寄存器；读操作通过地址选择器将状态寄存器送到 `PRDATA`。对于未映射地址或读写属性错误的访问，通过 `PSLVERR` 返回错误。在此基础上，实现了端口使能、读写权限配置、错误状态捕获和请求统计等功能。

这个模块主要用于将 AXI4 接口转换为 AHB-Lite 接口，使 crossbar 的输出端口能够连接低成本、顺序执行的 AHB 外设。由于 AXI 具有独立的 AW、W、AR 通道并支持多笔请求排队，而 AHB-Lite 同一时间只能顺序执行一个事务，因此我在 AXI 入口为 AW、AR 和 W 分别设置了 FIFO，用于吸收上游请求和缓解 AHB 侧反压；

### fifo深度

当前 AW 和 AR 请求 FIFO 默认深度为 2，因为地址 entry 比较小，而且 AHB 后端只有一个串行 executor；当一笔事务正在执行时，再缓存两笔后续请求，已经能够吸收短期的 AXI 请求突发，继续增加深度不会提高 AHB 的实际执行带宽，只会延后上游被反压的时间。

W FIFO需要单独考虑，因为一笔 AW 对应最多 `AWLEN+1` 拍数据，AXI4 最大是 256 拍。为了在 AHB 执行当前事务时，能够缓存 AW FIFO 中两笔后续写事务的数据，我将 W FIFO 默认配置为 `2×MAX_BURST_LEN`，即 512 拍。这样请求 FIFO 的排队能力和写数据 FIFO 基本匹配，同时通过 WREADY 在 FIFO 满时正常向上游反压。





















ICN中还可以挂载CHI的RNI节点，支持读写事务到CHI ReadOnce与WriteUniquePtl的转换。

从功能上看，该RNI是一个AXI到CHI的桥接模块；从CHI系统架构看，它也是一个RN-I协议端点，负责代表AXI侧I/O设备参与CHI事务和一致性访问。其整体架构如图所示，主要分为三个部分：读事务转换、写事务转换以及链路层控制。

首先介绍读事务转换。AXI总线上的AR控制信号会在AXI_BUS中打包，并通过握手写入AR FIFO。随后，segburst逐个处理FIFO中的AR请求，将其拆分为一个或多个不跨越64B Cacheline边界的CHI segment；只有当前Burst的所有segment都被下游接收后，才会弹出该FIFO项并开始处理下一笔AR事务。由于本设计面向128bit AXI、256bit CHI DAT和64B Cacheline的固定配置，因此选择三者的最大公共数据粒度16B作为bank大小。每个segment会包含地址以及三个Mask，其中分别用于标识起始bank，该segment覆盖的bank，原始AXI Burst最后一个Beat所在的bank。这些拆分的segment会被存放在下游的entry中，在存放的过程中会产生两条依赖链，分别是约束同ARID且同Cacheline的TXREQ发送的req chain以及约束同ARID事务返回AXI RDATA时不乱序的rdata chain。Entry准备完成后，ARCTRL会从满足发送条件的entry中仲裁一个用于组装TXREQ。候选entry按照QoS等级以及New/Retry状态分为四组，每组内部采用轮询仲裁，最终再按照“高QoS Retry、高QoS New、低QoS Retry、低QoS New”的固定优先级选出一个entry。其中，New候选是Req chain已经解除、尚未被选中发送且没有进入Retry流程的有效entry；Retry候选则是此前收到RetryAck，并且已经获得匹配PCrdGrant、具备重新发送资格的entry。选举出要发送请求的entry后进行REQ flit的组装，主要填写qos，srcid，tgtid等，其中opcode固定为readonce，TxnID采用本设计内部的编码方式，最高位固定为0表示读事务，低位填写entry 的 index，读size固定为64B，产生req flit后与链路层进行握手发送。在发送完readonce请求后，HN会返回2个dat flit（64/32），flit会进入rd buffer中，提取出data与resperr并根据flit中的txnid与dataid将数据存放在对应entry的对应bank中。数据返回后进行输出仲裁，Entry不需要等所有bank全部收齐，只要当前CTMASK指向的下一个待返回bank已经收到，并且该entry没有受到RDATA chain限制，就可以参与AXI RDATA输出仲裁。仲裁结束得到需要返回AXI RDATA的entry,bank索引，ARid等，将数据以及相应从相应bank中读出送入rpfifo中，并根据AXI size判断单个数据需要输出的个数，将数据再写入rdfifo中，只有最后一个展开Beat保留RLAST，rrd fifo与AXI R通道进行握手完成数据返回。

写请求与读请求在拆分segment时类似，区别在于产生的依赖链不同，AW entry分配时会建立三条依赖链，其基本匹配条件均为AWID相同：Req chain用于约束同AWID TXREQ的发送顺序；CompAck chain用于约束RNI向HN发送CompAck的顺序；BResp chain用于保证同AWID事务向AXI返回BResp时不会乱序。与此同时，拆分的segment会送入wr buffer将各个W Beat写入对应entry的对应16B bank，同时保存WSTRB信息。AWCTRL采用与读通路相同的四组仲裁选出entry，组装WriteUniquePtl请求，其中TxnID最高位置1表示写事务、低位为entry index。HN返回DBIDResp或CompDBIDResp后，Req chain解除；当entry已经获得DBID且W数据收齐后，即可独立参与TXDAT仲裁，wrbuffer将两个bank组装成一个256bit DAT Flit，并将WSTRB转换为CHI BE。数据发送完成并收到Comp后，若请求设置了ExpCompAck，则在CompAck chain解除后向HN发送CompAck。最后，当Comp、TXDAT和CompAck条件均满足且BResp chain解除后，将该segment的完成信息写入BResp FIFO；非末尾segment自动出队，只有原始AXI Burst最后一个segment产生BVALID/BRESP并与AXI B通道完成握手。

最后是链路层控制，主要负责LinkActive激活与退出握手，以及TXREQ、TXRSP、TXDAT、RXRSP和RXDAT五个CHI通道的Flit收发与L-Credit管理。其中，TX通道记录HN授予RNI的信用，只有链路处于RUN且信用可用时才能发送Flit，发送后信用计数减一；RX通道在链路进入RUN后向HN发布初始信用，每接收一个Flit后再补回一个信用。此外，链路层还负责仲裁读写通路的TXREQ、在信用不足时保持待发送Flit，以及过滤接收到的信用返回Flit。链路进入DEACTIVATE后停止普通LCRDV交换，TX通道通过LCRDRETURN Flit归还未使用信用，RX通道则接收并统计HN返回的信用；只有信用全部收回后，链路才允许进入STOP状态。



# NVME OVER TCP 硬卸载关键技术研究

这个项目是一个面向Controller侧的NVMe/TCP硬件卸载前端，主要完成8路连接的独立缓存、公平调度、PDU读取、协议解析和部分控制响应生成。整个设计采用512-bit数据通路，目标频率是200 MHz。

从图的左侧开始，TCP接收数据通过`wr_data、wr_keep、wr_last和wr_conn`进入异步缓存模块。缓存按照连接号划分为8个独立区域，每条连接分别维护工作写指针、提交写指针和读指针。工作写指针随每个有效数据拍递增，当`wr_last`到达时，将更新后的写位置保存到提交写指针，表示一帧完整PDU已经写入。随后，提交写指针经过格雷码编码和跨时钟同步进入核心读取时钟域，并与对应连接的读指针比较产生`pending`信号（不相等说明有新的PDU写入)，以此实现Doorbell报文就绪通知。Reader完成数据消费后更新读指针，读指针再反向同步到写时钟域，用于缓存满判断。通过工作写指针和提交写指针分离，能够保证Doorbell只发布完整PDU，避免读侧读取到尚未写完的报文。

`pending[7:0]`送入轮询调度器。调度器采用Round-Robin策略，从存在完整PDU的连接中选择一路，并通过`grant_valid和grant_id`授权Reader读取。一次授权会保持到当前PDU全部读完，Reader返回`grant_done`后，调度器才更新优先级并选择下一条连接，从而保证多连接之间的公平性，同时避免不同连接的PDU发生交叉。

`nvmetcp_pdu_reader`根据授权连接向缓存发起读请求。它在首拍提取`PLEN`，计算整个PDU需要读取的512-bit拍数，并检查末拍的`rd_last`和`rd_keep`是否与实际长度匹配。读取接口支持ready/valid背压，下游不能接收时会保持当前数据不变。

随后数据进入`nvmetcp_parser`。Parser解析首拍中的Type、Flags、HLEN、PDO和PLEN，并支持ICReq、ICResp、TermReq、CapsuleCmd、CapsuleResp、H2CData、C2HData和R2T等9类PDU。根据不同类型，它还会提取CCCID、TTAG、数据偏移和数据长度等字段，并完成头部长度、PDU长度、Flags以及字段对齐检查。

Parser有两条主要输出：一条是`payload_*`，将完整原始PDU转发给外部命令或数据处理模块；另一条是`action_meta`，输出结构化解析结果。对于合法ICReq，内部Response Generator自动生成ICResp；如果检测到协议错误，则生成对应方向的TermReq。CapsuleCmd和H2CData等正常业务报文直接输出给外部模块。



# 项目中优化时序的方法(Performance)：

对于多级流水线设计,使用skidbuffer级联,减少了ready回传的路径长度,极大优化了时序。

在DMA设计中，状态机多采用独热码编码，使用assign is_s1 = state[1]这种方式判断当前状态，缩短了组合逻辑链路长度。

在802.1AS的计算的设计中,使用右移的方式来实现除2的功能,降低组合逻辑链优化时序,在BMCA算法中,使用流水拆分大位宽的向量比较,优化时序.

在涉及到优先级编码器的设计中，使用二叉树的形式进行设计。

我曾在vivado上做过一个小实验，对于8bit的输入数据寻找最低位LSB，分别采用casez的优先级编码器以及二叉树的实现方式，接入同一个时序逻辑中进行综合，使用SDC约束不断提高时钟频率，发现最后在时钟周期为1.04ns的情况下，casez的优先级编码方式发生了违例，二叉树的实现却没有。



在AXI-Crossbar的设计中，根据各个通道的流量特征，我没有统一使用同一种寄存器切片。地址通道 AW 和 AR 每个 burst 只传输一次，但译码和仲裁路径较长，因此使用 simple buffer 切断关键路径；W 和 R 是连续 beat 的高吞吐数据通道，为了在反压下避免 bubble，使用 skid buffer；B 响应通道频率较低，使用 simple buffer 即可。其他路径优先 bypass，避免额外面积和延迟。最终是根据 STA 结果，在面积、频率、吞吐率和 latency 之间做权衡。



# 项目中优化面积的方法(Area)：

在 RTL 编写时，我会根据每个寄存器或中间信号的实际取值范围来定义位宽,如根据最大取值范围使用 `$clog2` 参数化计算寄存器位宽。

在 PHY 侧跨时钟域设计中，恢复时钟和系统时钟都是 125MHz，上下游数据位宽相同，所以平均吞吐率是相同的。从理论估算看，较小深度就可以满足，例如 6 左右；但考虑到 Gray 指针设计通常要求深度取 2 的幂，同时为了留一定的裕量，最终将 FIFO 深度设置为 16。

在802.1AS的计算的设计中,使用右移的方式来实现除2的功能.优化面积。

在PCIe DMA的乱序返回的RAM设计中,我可以根据MRRS计算出每个Tag可以返回的最大数据量,对fifo深度进行最小裕量设置。

对于描述符的深度设置,可以根据驱动侧参数进行合理配置,确保能够完整存储一次描述符的最大值







# 项目中优化功耗的方法(Power)：











# AI提问

**1. 你先怎么整体介绍这个项目？**
“这个项目是一个基于 PCIe Gen2 x8 的多通道 SG-DMA 引擎，目标是在 FPGA 和主机内存之间实现高带宽、低 CPU 参与度的数据搬运。我主要负责 TLP 处理引擎、completion 乱序重排、基于 credit 的流控、多通道 DMA 通路搭建，以及驱动和 RTL 的联调闭环。”

**2. 为什么要做 SG-DMA，而不是普通连续 DMA？**
“因为主机用户态 buffer 在物理上通常是不连续的，不能假设是一整段连续物理地址。驱动需要先 pin 页并生成 SG 表，FPGA 再按 SG 描述符分段访问主机内存。这样既能支持大块数据，也能兼容真实操作系统里的离散内存布局。”

**3. 这个架构你是怎么分层的？**
“我把它分成三层。最左边是 PCIe 协议接入层，负责 TLP 收发和配置空间对接；中间是公共基础设施，包括 BAR 寄存器、中断、reorder queue、credit flow control 和发送仲裁；右边是按 channel 复制的数据通路，每个 channel 分成上报和下发两条链路。”

**4. TX/RX 处理引擎是怎么做全流水解析的？**
“接收侧先由 rx_engine 把 TLP + valid + SOP/EOP + offset 全部打一条统一延迟链，然后分别送给 rxr_engine 和 rxc_engine 去解析 request 和 completion。发送侧则是 txr_engine 和 txc_engine 分别负责 request 和 completion 的组包，最后统一进入发送仲裁。这样把协议解析拆成了前端对齐和后端专用解析两部分，更利于 250MHz 下时序收敛。”

**5. 为什么 RX 要拆成 rxr_engine 和 rxc_engine？**
“因为 request 和 completion 的格式差异很大。request 这边要区分 3DW/4DW、MRd/MWr，还要做地址重组；completion 这边更关注 tag、byte count、lower address 和 payload 对齐。分开做以后，控制逻辑更清晰，时序压力也更小。”

**6. 为什么需要 reorder queue？**
“因为多个 MRd 发出去之后，主机返回的 completion 可能乱序到达，不能直接按到达顺序送给后级。我这里是先按 tag 把数据写入分段 RAM，再由输出状态机按 tag 顺序扫描并恢复成连续的 128bit 数据流，这样后级看到的就是逻辑上有序的数据。”

**7. 为什么 reorder queue 用 4 个 32bit RAM bank？**
“因为当前 PCIe 用户侧宽度是 128bit，也就是 4 个 DW。拆成 4 个 32bit bank 后，同一地址并行读取就能自然拼成一个 128bit beat，这样既方便按 DW 粒度做 shift，也方便和后面的 packet_128、FIFO 这类模块对齐。”

**8. reorder_queue_output 为什么要对 4DW、8DW 单独处理？**
“因为当前输出粒度就是 128bit = 4DW/拍，所以 4DW 和 8DW 分别对应单拍包和双拍包边界。对短包做特判可以减少状态切换，缩短尾拍处理路径；更长的包再进入通用 streaming 路径，这样结构更清晰，控制也更稳。”

**9. 你这个乱序输出状态机怎么讲？**
“我一般分四个状态讲。S0 是等待并扫描完成 tag；S2 是首拍输出并判断总长度；如果总长度大于 8DW，就进 S3 做中间拍流式输出；最后进 S1 做尾拍输出和 tag clear。它的本质是把已经乱序收回来的 completion，重新按 tag 顺序、按 128bit 拍吐给后级。”

**10. 发送仲裁是怎么做的？**
“发送仲裁在 tx_mux_arbiter 里做，当前是按包粒度实现 WRR。它不是按 beat 切换，而是只有在一个 TLP 发完，也就是 EOP 时，才允许在 TXR 和 TXC 之间切换。这样既保证 request 吞吐优先，也不会把 completion 长时间饿死。”

**12. 12 个通道你是怎么支持的？**
“顶层是参数化多通道架构，emoki 里通过 generate 方式按 C_NUM_CHNL 实例化多个 emoki_channel，每个 channel 都有独立的 TX/RX 事务状态、SG 处理和用户侧接口；中间的 reorder queue、interrupt、tx_multiplexer 这些是共享资源。也就是说，我做的是‘通道私有状态 + 公共仲裁资源’的结构。”

**13. 12 通道验证这件事如果面试官追问，你怎么答？**
“我会说我不仅是参数上支持，而是在顶层实际完成了 12 通道例化，并验证了多通道下的基本收发链路、中断上报和共享资源访问。验证重点是检查多通道同时存在时，reorder、共享发送路径和中断向量不会互相串扰。”

**14. 中断模块你怎么讲？**
“中断模块把每个 channel 的 5 类关键事件打包成向量，包括 TX 事务开始、TX SG 收到、TX 完成、RX SG 收到、RX 完成。12 个通道一共 60 bit，分两组 32bit vector 上报给主机。这样驱动通过读向量寄存器就能知道哪个通道发生了哪类事件。”

**15. 为什么中断向量要分两组 32bit？**
“因为 12 个通道、每通道 5 类事件，总共是 60 个 bit，一组 32bit 放不下，所以拆成两组 vector。这样结构简单，也方便驱动按位解析。”

**16. Credit flow control 是怎么设计的？**
“我做的是保守型 credit 流控。每发一个 MRd，就先预留最坏情况下接收该 completion 所需的 header/data 资源；等这笔 completion 被完整处理后，再归还 credit。这样能避免接收缓冲区被打满，在复杂并发下更稳。”

**17. 为什么你的流控是保守型，而不是精确型？**
“因为精确型虽然理论并发更高，但控制复杂度更大，而且更容易在边界情况下出错。保守型设计用 MRRS、RCB 和 completion credit 上限估最坏占用，牺牲一点极限吞吐，换更稳的系统行为，我觉得在工程上是更合理的取舍。”

**18. 上报链路怎么讲？**
“上报是 FPGA 先发出事务通知，主机驱动收到后准备接收 buffer 的 SG 表，FPGA 再先把 SG 表读回来，再根据 SG 描述符把用户数据写回主机内存。这条链路的核心模块是 tx_port_channel_gate、tx_port_monitor、tx_port_writer、tx_port_buffer 和共享发送路径。”

**19. 下发链路怎么讲？**
“下发是驱动先准备发送 buffer 的 SG 表，FPGA 先把 SG 表读回来，再按 SG 描述符发 MRd 去主机取真正的数据，completion 回来后先进入 reorder queue，重排之后再送到对应用户通道。核心在 rx_port、sg_list_req、reorder_queue 和 rx_port_channel_gate。”

**20. tx_port_buffer 为什么需要？**
“因为用户数据是连续流，而 tx_port_writer 切出来的每个写请求长度不一定是 4DW 对齐。tx_port_buffer 的作用就是把前一个请求尾部剩下的非 4DW 数据和下一个请求的数据重新拼起来，确保下游每次看到的都是当前 PCIe 写请求应该发出的 128bit 数据。”

**21. 这套 DMA 是 byte 级的吗？**
“当前这版不是 byte-native DMA，而是以 32bit DW 为单位。驱动 API、SG 长度和 RTL 通道接口都是按 DW 定义的，所以我会把它定义成 DW 粒度的 SG-DMA 引擎。”

**22. 83% 带宽利用率你怎么解释？**
“Gen2 x8 理论原始带宽大约 4GB/s，83% 对应有效 payload 吞吐大约 3.3GB/s。考虑到 8b/10b 编码、TLP header、SG 读取、completion 重排和驱动交互，这个结果是比较合理的。它说明系统不是只停留在功能正确，而是已经做到了比较高的链路利用率。”

**23. 你这个项目最难的点是什么？**
“我觉得最难的不是写某一个状态机，而是把协议层、数据路径、乱序重排、流控和驱动协同真正串起来。尤其是多通道下，问题不再是单点功能，而是共享资源之间会不会互相影响，这部分我花了很多精力去梳理和验证。”

**24. 如果让你总结这个项目的工程价值，你怎么说？**
“我会说它体现的是系统级 RTL 设计能力。不是单个模块做对了就结束，而是要把 TLP、DMA、SG、驱动、中断、流控和多通道扩展放到同一个系统里考虑，最后还能在 250MHz 下收敛并跑出较高带宽。”

**25. 最后一问：你在这个项目里最能体现个人能力的部分是什么？**
“我最想强调的是两点。第一，我能把复杂协议问题拆成清晰的模块边界，比如解析、重排、仲裁、流控和通道私有逻辑。第二，我不是只会写 RTL，而是能把驱动和硬件一起看，把整条 DMA 交互流程真正跑通。”













