# 自我介绍

面试官您好，我叫王昊宇，本科与硕士均就读于西安电子科技大学通信工程学院，本科综合成绩 95.91，获得保研资格；硕士阶段就读于空天地一体化综合业务网国家重点实验室，GPA3.8，本硕期间多次获得奖学金以及优秀学生称号，竞赛方面获得三项国家级奖项和三项省市级奖项，还参与申请了七项国家发明专利。

实习期间在华为海思图灵处理器芯片开发部独立完成了虚拟项目RISC-V Core的数字前端全流程开发。

我的项目经历主要集中在高速接口以及片上总线等方向，我完全负责 802.1AS 协议的硬件卸载及其测试仪设计，同时设计了一个包含AXI crossbar，APB配置接口，AHB-lite桥接的 ICN，并集成了CHI RN-I 节点。

以上就是我的自我介绍，希望能加入贵团队，谢谢！

# 实习经历

**项目介绍：在华为海思图灵处理器芯片开发部实习期间,我完成了一个基于 RV32I 指令集的 RISC-V Core 开发，这次实习让我完整经历了一次数字前端开发流程,从前期的架构设计、RTL 实现,到后续的功能验证以及综合和 PPA 优化。整个 Core 最初采用五级流水线,后续为了突破频率瓶颈,我把取指阶段拆分为 IF0 和 IF1,最终优化为六级流水线。一条指令首先在 IFU 中完成取值,然后进入IDU进行译码和寄存器读取,同时完成 Hazard 检测和数据前递;接着进入 EXU 完成运算、分支判断或者访存地址计算;如果是 Load 指令,会继续进入 MEMU访问数据存储器,最后在 WB 阶段写回寄存器。整个过程中,CTRL 模块统一负责流水线的 Stall、Flush 和异常控制。**

**在这个项目中,我主要做了三件事，第一是 RTL 设计,实现各级流水线设计，数据前递、Load-Use Hazard处理、异常处理以及流水线控制;第二是 UVM 验证,包括冒烟测试、回归测试以及门级仿真,通过 Reference Model 和 Checker 自动比对,分析定位和修复问题;第三是 PPA 优化,基于 DC timing report 分析关键路径,基于Prime多轮迭代 RTL,包括拆分 IF 阶段、增加时钟门控、组合逻辑和复位逻辑优化等。最终,主频从 1.6GHz 提升到 2.34GHz,功耗从1.421 毫瓦降低到1.089 毫瓦,并完成了 SpyGlass Lint、UVM 回归和门级仿真,完整经历了数字前端开发流程。**

core的设计上整体采用五级流水线设计，后由于关键路径无法突破，将IF分为两级，变为六级流水线，IF阶段采用 always not taken的分支预测策略，即固定向下取指，pc固定加4。IFU还需要接受来自ctrl的flush与stall控制信号，接收flush信号时将冲刷流水线上的数据，为了降低功耗仅拉低valid信号，数据通路保持不变，接收stall信号时应保持流水线数据不变。 

对于ID阶段，主要需要考虑RAW冲突以及load use冒险，RAW冲突就是后一条指令 需要读取前面指令的计 算结果，但前面指令还没把结果写回寄存器，导致后一条指令读到了“旧值”，即后面的指令的寄存器需要 用到前级指令的写入寄存器，由于在WB阶段才进行寄存器的写回，若没有数据前递，就需要暂停流水线，降低性能，若ID阶段需要的寄存器处于EX,MEM,WB阶段，可以直接将数据前递，这样可以不用暂停流水，优先级为EX>MEM>WB。当指令为load指令时，由于在EX阶段才对DMEM进行读取，读数据在MEM阶段得到， 若指令在ID阶段发现上一条指令为访存且寄存器相同，就需要stall住流水线，直到仿存指令处于MEM阶段时 才得到数据进行前递，但是考虑到时序，数据从DMEM读出有较大的读出时延再经过较长的前递组合逻辑， 时序会很差，因此stall住流水直到访存指令处于WB阶段时再进行前递，因此会stall两拍。IDU还会译码出EX 需要的操作数，操作码，以及读写DMEM的标识等。

EXU根据操作码对操作数进行不同的运算，对于load与store指令，产生DMEM的使能与地址等，对于跳转 指令，产生跳转使能与跳转地址。

MEMU阶段获取DMEM的读数据，将读数据以及要写的寄存器地址与数据送入MEM到WB的pipe中。 

WB阶段根据控制信号判断写回寄存器的数据来自DMEM还是寄存器数据，若为DMEM根据地址信号以及指令类型对128位数据进行索引与高位拓展得到写入数据，若为寄存器数据就直接赋值即可。 

CTRL模块的第一个作用就是对启动信号进行跨时钟域，由于外部输入的start_pulse与start_pc同拍给出， 并且start_pc后面保持不变，所以这里采用DMUX的方法进行跨时钟域，将脉冲同步过来后再根据脉冲对 start_pc进行采样，接收到来自EX的跳转使能输出flush去清空IF与ID的流水，接收来自ID阶段的load use hazard标识输出stall去暂停IF与ID的流水。

PPA迭代：IF模块在PPA优化过程中经历了三版迭代，第一版设计为pc值打拍之后作为地址给入IMEM取指，指令在下一拍读出，再将pc值给入IF-ID的pipe中，此时pc 与instr同时给到ID阶段，这版设计的关键路径在IMEM的读出时延再加上译码以及数据前递的组合延迟，导致频率的上限为1.6Ghz，对架构进行更改，将给IMEM的地址变为pc的前一级，这样IMEM的指令会提前一拍读出，此时将if_pc与instr都给入IF-ID的pipe中，吸收了IMEM的读延迟，使用此架构最高频率可达2.14G Hz，但由于pc经过较长的mux逻辑链没有打拍就给到IMEM的地址端，此时关键路径变为从产生跳转使能到 跳转地址给入IMEM的读地址,为了冲击更高的频率，将IF阶段分为两级IF0，IF1，将IF0打拍后的pc给入IMEM，读指令在IF1输出，将if1_pc与instr同时给入IF-ID的pipe中，这样既吸收了IMEM的读出时延，又吸收了 pc的长组合逻辑时延，此架构下最高频率可达2.34GHz。

关于异常：core分为两种异常，一种是PC非对齐异常，一种是指令异常。PC非对齐异常会在IF阶段判断， 并随着流水线流向下级，与EX阶段的跳转pc非对齐整合，异常流到MEM阶段时，说明异常指令前级已执行 完并retire，此时ctrl发出flush信号清空IF,ID,EX，并拉低core_running标识，CPU进入休眠状态，同时对外输出异常类型以及异常的pc值。对于指令异常，在ID阶段产生，若指令译码失败说明指令非法，同样跟随流水线向下传递，到MEM阶段对外上报错误并冲刷流水。 关于WFI指令与异常的处理相似，ID阶段译码出WFI指令后，流到MEM关闭core并将core_status的wfi位置高。 DC综合工艺为TSMC N6工艺，功耗使用snopysis primetimepx进行分析，输入门仿波形，门级网表，约束。

core的UVM验证环境如图所示，harness将DUT，interface以及验证组件全部包含进去，cfg_interface用于向DUT输出驱动信号（启动脉冲与pc，coreconfig），通过虚拟接口以UVM寄信的方式与cfg_agent相连，同时cfg_ mon将驱动信号采集一份给RM作为启动信号。chk_interface用于采集DUT的信号，同样通过虚拟接口与 仿真环境相连，采集六路比较信号，包含core外部输出信号core_status,core_exceptions,exceptions_pc， 跳转pc，rfu数据，DMEM数据，将采集信号输出给checker与RM的输出信号进行比对，判断是否正确，数 据通路间通过fifo连接。各条指令在inst_gen中定义，在inst_gen_sequence中将指令组成序列，init_xn_seq写入具体指令内容并将每条指令通过后门写入到DUT与RM的IMEM中。 checker的比较：对于每个比较序列，对DUT的队列进行判断，若队列非空但RM的队列为空，说明DUT提前于RM产生了数据，输出FATAL错误退出仿真，若均非空则进行比较，如果数值不同输出ERROR，累计错误数到达上限时，退出仿真。

### 问题汇总

#### DC综合使用什么工艺？

使用TSMC N6工艺库进行综合，面积约为3900平方微米。

### 功耗是如何测量的？

> 我先使用 DC 综合得到门级网表和 SDC，然后使用 VCS 对门级网表进行仿真，产生波形文件。在 PrimeTime PX 中读入仿真波形、门级网表、SDC，进行分析，得到2internal power、switching power、leakage power 以及 total power。

### 优化PPA的方法？

**根据 DC 的 Timing Report 优化**：分析关键路径，调整组合逻辑级数。

**合并 `if` 条件**：减少由层层条件判断产生的 MUX 级数，如跳转指令branch1与jump有if else逻辑优化为或门的设计。

**精简复位逻辑**：尽量**只复位控制通路，不复位数据通路**，减少复位网络和触发器开销。

**根据回归测试的条件覆盖率**，删除逻辑中的冗余/不必要条件。

**使用更严格的门控条件**，进一步降低信号翻转率，优化动态功耗。

**为了时序，不在 MEM 阶段做 Load 前递**，避免形成过长的 Load 数据前递路径。

**复用 ALU**：例如左右移逻辑复用，以降低面积，但代价是可能引入额外 MUX。

**分析时序报告，消除多余的 MUX 级数**。

**减少嵌套 `case`**：能提前计算的逻辑提到 `case` 外面，或者将多个 `case` 合并。

**调整 `if-else` 优先级**：将**组合逻辑链较长的条件提前到第一级 `if`**，减少关键路径上的 MUX 级数。

**手动加入 ICG 单元**，门控时钟可以按照模块划分，进一步降低时钟动态功耗。

**按需使能 IMEM/DMEM**：只有真正需要访问 IMEM 或 DMEM 时才拉高 `CE`。

**避免直接对 128-bit 大位宽信号做选择**：可以先 MUX 出 16-bit 等小位宽数据，再进行统一位宽扩展，降低大位宽 MUX 的面积和延迟。

### RV32I指令集

![image-20260812221616835](C:\Users\wanghaoyu\AppData\Roaming\Typora\typora-user-images\image-20260812221616835.png)



# 项目经历

# TSN时间同步IP

**项目介绍：这个项目是与西安631研究所合作的 TSN 测试系统研究课题。针对 TSN 网络对高精度时间同步和协议一致性测试的需求,我负责根据协议标准实现可用于端系统和交换机的时间同步IP,并基于研究所对TSN交换机的时间同步测试需求使用Xilinx K7系列开发板实现了时间同步测试仪。整个系统主要分为两部分:一部分是时间同步IP,负责完成 802.1AS 协议的硬件卸载;另一部分是硬件测试系统,负责对时间同步 IP 进行功能验证和性能测试。测试过程中,主机通过 PCIe 向 FPGA 下发测试配置,测试仪按照 Avnu 规范完成报文的发送与解析,测试数据再通过 PCIe SG-DMA 上传,DDR 负责缓存和流量回放。在这个项目中,我主要负责时间同步 IP 的设计,实现802.1AS 协议的硬件卸载,包括 PTP 时间戳获取、链路时延测量以及 BMCA 主时钟选举。第二部分是硬件测试系统的设计与实现,包括测试仪、PCIe SG-DMA 以及 DDR 数据通路,实现协议一致性测试、错帧注入测试以及网络探针损伤测试等功能。**





TSN 网络是一个时间敏感网络，要求关键报文不仅能够在规定时间内到达，且具有较小的时延抖动，如果网络中的各个端节点和交换机的时间不一致，上一级交换机的发送窗口和下一级交换机的转发窗口可能无法正确衔接，报文可能需要等待下一个调度周期，甚至被时间门禁过滤。802.1AS为TSN网络中的各个节点提供统一的时间基准，是时间感知调度机制正常运行的基础。

传统 Linux ptp4l 等方案容易受系统调度、协议栈开销影响，引入不可控时间抖动，同时增加 CPU 负担，我将协议卸载到硬件，设计了适用于端节点以及交换机的两个IP，同时基于协议测试标准设计并实现了IEEE802.1AS同步测试仪，代码总数两万九千行。

802.1AS交互流程如下：

第一步是选主：网络中的各个节点会交互Announce报文，报文内会携带自身的时钟信息，节点接收到来自别的节点的Announce报文后会与自身时钟信息比较，并更新端口状态为主或从，通过选举整个网络形成一个以主时钟为根节点的树状结构。

第二步是测延迟：节点间交互Pdelay与Presp报文，发送端记录请求发送时间 T1 和响应接收时间 T4，对端记录请求接收时间 T2 和响应发送时间 T3，用总往返时间减去对端处理时间，再除以2，得到单向链路时延。

第三步是校时钟：主时钟会周期性发送 Sync 报文，并通过 Follow_Up 携带sync报文的发送时间戳。从节点收到后Sync报文标记接收时间点，在从Follow_Up提取到Sync报文的发送时间点，这样主节点告诉从节点“我是什么时候发的”，从节点知道“我是什么时候收到的”，再扣掉链路传输时间，就能算出两个时钟差了多少，然后把这个误差校正掉。

接收侧，报文从PHY进入时，在靠近物理接口的位置捕获硬件时间戳。报文和时间戳通过CDC进入gPTP核心。帧解析模块进行分流解析，获取相应的时间点以及来自别的节点的时间参数。

计算侧主要完成两件事。第一是通过Pdelay机制获得本端口的点到点链路延迟。硬件保存Pdelay事务相关的T1到T4，检查sequenceId和对端身份匹配后，再计算linkDelay。第二是通过Sync和Follow_Up获得主时钟的精确发送时间，并结合本地接收时间、linkDelay和correctionField计算本地时钟偏差。计算结果交给Time manager，由它维护本地系统时间并产生相位或频率调整控制量。

Announce报文则进入BMCA。BMCA按照priority1、clockClass、clockAccuracy、priority2、clockIdentity等字段逐级比较，选择最佳主时钟，并决定端口处于Master还是Slave状态。由于比较字段位宽较大比较多，我在架构中将其设计为流水化比较过程。

发送侧由Port manager和Frame generator构成。Port manager根据端口状态和报文周期产生Sync、Pdelay和Announce触发。Frame generator维护不同报文的模板和动态字段，填入sequenceId、时间戳和correctionField，再通过AXI-Stream接口发送。对于两步Sync，Sync发出时捕获的发送时间戳会用于生成对应的Follow_Up，因此发送时间戳和sequenceId也必须严格匹配。

项目中主要有三个难点。第一个是PHY时钟域中的时间戳与系统时钟域中的报文必须严格对应，所以需要通过带帧标识的元数据和异步FIFO完成CDC，而不是简单同步一个pulse。第二个是Sync和Pdelay都涉及多个报文，需要保存事务上下文，并处理sequenceId不匹配、超时、重复和乱序。第三个是Boundary Clock发生最佳端口切换时，必须协调端口状态、系统时钟和报文调度，防止新旧Slave同时修改系统时间。

# PCIe DMA架构

**DMA工作流程：DMA 分为控制面、描述符面和数据面。驱动通过 BAR下发控制信息，通过 MSI接收状态；用户 buffer经过 `dma_map_sg` 转换成设备可访问的 DMA地址，SG描述符保存在Host共享内存中，FPGA通过MRd读取。下行时用户调用 `fpga_send`，驱动下发 RX事务和SG地址，FPGA根据描述符发送多笔MRd，Host返回的CplD经过tag乱序缓存恢复顺序，再送入FPGA用户FIFO。上行时FPGA先通过TXN中断通知驱动，`fpga_recv` 建立 `DMA_FROM_DEVICE` SG映射并下发TX SG地址，FPGA读取描述符后发送MWr直接写入用户页。每批SG描述符消费完成后用 `SG_BUF_RECVD` 通知驱动流水更新，整个事务完成后再用 `TXN_DONE` 返回实际传输长度。因此控制流走BAR和中断，描述符走SG DMA，真正数据分别走MRd/CplD和MWr。**



如图所示是PCIe DMA的架构图，本DMA是基于Xilinx的7系列PCIe硬核进行开发的，硬核会将TLP报文以AXIS的格式吐出， 首先来介绍一下接收通路，数据位宽固定为128bit，将接收TLP根据fmt分类，Mwr/Mrd进入RXR_engine，Cpld进入RXC engine，报文解析分为多种情况考虑，不发生跨位的3DW或4DW报头，发生跨位的3DW或者4DW报头，将包头的meta数据解析出来之后，随数据通路一起流出。 

对于DMA下发来说，驱动首先进行pio写，告知DMA引擎本次下发的数据长度，offset等信息，并写入sglist的地址以及长度，rxsglist req开始根据MRRS与4K边界计算发送长度以及对应地址，sglist获取完成之后上报中断，告知可以进行下一次 sglist的下发，对于DMA上报，上报的sglist获取流程与下发相同，接收到描述符后，reader模块进行读请求产生，从sglistfifo中读取描述符，根据描述符长度，MRRS与4K边界三个约束得到读请求长度与地址，因此共计会产生三种不同作用的读请求，统一送入mux中进行仲裁，仲裁获胜的请求，会带着请求标识给到tx_mux进行统一仲裁，将通道号与请求标识组装与Tag管理模块进行握手获取Tag，这里还会收到流控的限制，同时Tag管理模块会记录本Tag的通道以及标识（作用），将发送信号送入TXR_engine中发送Mrd事务，返回的cpld经过RXC_engine预解析之后送入乱序管理模块。

当下发完成且用户侧读取完所有数据后，产生下发完成中断并上报实际下发数据长度。接收到Tag完成标识即cpld中的length与bytecount相同时,说明本Tag的读数据已返回完毕可以被reorder_output调度出去,为了实现乱序返回顺序读出reorder_output模块必须按照tag顺序进行调度,也就是说前级Tag没有返回完成时,即使后面的Tag已经完成数据收集也不可以读出,数据读出时将Tag通过Tag管理模块进行映射,得到数据属于哪个通道的下发数据或者rxsglist数据或者txsglist数据，数据经过打包模块后分别存入对应的fifo中，其中数据fifo为异步fifo，将下发数据由DMA时钟域跨至用户时钟域。

对于DMA上报，首先用户通道拉高TX上报标识并将上报长度，偏移地址给出，通过gate打包后，变为一个129bit位宽的数据，将长度与偏移作为一个上报包的报头最高位置0，紧跟着数据信号，最高位置1，将数据给入异步fifo中进行跨时钟域，跨入DMA引擎时钟域，monitor模块对异步fifo的读出数据进行分析，根据控制位标识解析出本次上报的数据长度偏等控制信号与上报数据，DMA上报信息与writer握手，产生中断信号，数据信号被存入txbuffer中等待调度，主机接收到上报中断后进行寄存器读，获得上报长度以及偏移，根据上报长度产生描述符，DMA引擎以与DMA下发同样的方式获取描述符并存入txsglist fifo中，portwriter读取txsglistfifo中的描述符，结合描述符长度，MPS以及4K边界获取本次上报的长度以及地址，当txbuffer中存储的数据量满足此次产生的上报请求的长度时，将此上报请求发出，给到tx_multiplexer中参与仲裁输出，被选举就将上报地址，长度等头部信息以及读数据给入TXR_engine进行发送。上报完成后产生上报完成中断，主机接收到中断后读取实际上报的长度。

关于流控设计，流控通过限制Mrd的发送来实现流控，PCIe为信用流控，flow_control模块维护一个已使用信用值，考虑极端情况，根据MRRS以及RCB参数计算需要返回的最大cpld包数，根据MRRS计算数据信用值，每4DW为一个数据credit，每成功发送一个Mrd就将头部信用与数据信用加上，每完整收回一个Tag的数据就减去相应的信用值，同时硬核会输出当前EP设备的可用头部信用与数据信用，若信用不够则不允许发送当前Mrd，必须等待前级Mrd完全返回数据之后才允许重新发送。

关于中断，使用中断聚合的方式，当有任意通道产生中断，与硬核进行中断握手，host接收到中断会向下读取中断寄存器，读取的同时消除中断标识位，再根据中断寄存器的值进行相应操作。

### 乱序返回的设计方法：

乱序输入处理模块的主要作用是根据 Cpld携带的 tag，把数据分发到对应的独立缓冲区，同时维护每个tag已经接收的数据量和下一次写入位置。由于DMA引擎以DW作为粒度，所以我的乱序缓存采用四路 Bank 结构，由四个 32 bit 位宽、1024 深度的 RAM 组成。四个 RAM 对应 PCIe 128 bit 数据总线中的四个 DW。系统支持 32 个外部 Tag，每个 Tag 预留 512 Byte，也就是 32 个 128 bit 存储位置。因此每个 RAM 的深度是 32 乘以 32，等于 1024。每个 Tag 的存储空间分布在4个RAM中，地址。

它的输入主要包括128-bit的 cpld 数据、4-bit的 DW有效标志DATA_EN、包首偏移、tag，以及当前 MRd是否接收完成的标志。

设计上为了实现高频下的时序收敛，采用五级流水线的方式，第一级读取tag的count ram，第二级得到当前tag已存储的DW数量，加上当前的有效DW数得到当前Tag更新后的DW数，后写回count ram中，同时对于DW数模4，计算偏移值，第三级得到当前偏移值，对当前的DW有效标识做循环移位同时读取pos ram获取当前4个bank的相对写地址，第四级得到循环移位后的DW有效标识，计算数据信号的左移以及右移的具体值，同时对相对写地址进行更新后写回ram中并计算当前Tag的基地址，第五级得到数据左移以及右移的具体值，对数据进行移位同时将相对地址与基地址相加得到4个bank的写地址同时

这里还有一个重要的流水相关问题：同一个tag的多个 CplD可能背靠背到达，但count RAM和position RAM存在读写延迟。如果后一拍直接读取RAM，可能读到还没更新的旧值。

所以我在count和position两条路径上都做了前递旁路。如果当前tag和前一拍相同，就直接使用前一拍流水线计算出的最新状态；如果和前两拍相同，就使用前两拍保存的状态；只有不存在相关时才使用RAM读出的值。这个机制类似处理器流水线的RAW冲突时的数据前递，可以保证同tag连续返回时count和写地址仍然正确。

## 带宽测试方法





# IEEE 802.1AS同步测试仪架构

同步测试仪的主体架构如图所示，上位机通过 PIO 下发测试配置，配置内容包括端口模式、时钟参数、测试时长以及每个端口的测试表项等；根据BAR偏移地址将数据分流到各个端口的独有的配置寄存器和错帧配置表，所以系统支持四端口并行测试。

每个端口内部我设计成“双数据面”结构：一个是协议一致性测试平面，另一个是故障注入平面。协议一致性测试平面在终端IP的基础上，添加了同步寄存器数据上报以及通过LSFR伪随机数生成模块实现的百分比随机丢帧功能来实现响应的测试功能。

测试通路则由表项驱动，表项解析模块读取fifo中的配置数据并按照格式进行解析，得到发帧的时间点，发帧数，帧类型以及错误码等信息，交给测试帧生成模块进行错帧的产生，同时在相应的时间点启动 monitior 模块进行监管，判断DUT是否对错帧进行响应。

我还在靠近物理层的地方做了探针故障注入设计，可以通过配置端口工作模式让端口 0/1、端口 2/3 实现透明转发，然后将测试板卡以探针的形式接入进网络中，在转发过程中通过探针修改模块对 gPTP 指定字段做在线篡改，比如 MAC地址，帧类型等，切换逻辑只在帧间空闲窗口生效，避免中途切模式造成断帧。

#### 丢帧设计

使用LSFR实现 10%随机丢帧，16bit位宽数据最大值65535：

```
reg [15:0] lfsr;
wire       random_drop_hit;

always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        lfsr <= 16'hACE1; // 赋非零初值
    end else begin
        // 16位 Galois LFSR (抽头: 16, 14, 13, 11)
        lfsr <= {lfsr[14:0], lfsr[15] ^ lfsr[13] ^ lfsr[12] ^ lfsr[10]};
    end
end

// 10% 概率阈值: 65536 * 10% ≈ 6553
assign random_drop_hit = random_drop_en & (lfsr < 16'd6553); 
```

注意复位值不能够为0，要不然会导致数值一直为0。

<img src="C:\Users\wanghaoyu\AppData\Roaming\Typora\typora-user-images\image-20260422204956404.png" alt="image-20260422204956404" style="zoom: 80%;" />

#### 万兆测试仪设计：

发送方向上，原模块输出8位、125 MHz的GMII格式数据。系统先去除原有的前导码和FCS，再通过异步FIFO完成从125 MHz到156.25 MHz的跨时钟域处理，同时将8位数据拼接为64位，并生成SOP、EOP和有效字节数等控制信号。随后，64位数据送入10G MAC，由MAC重新生成前导码、FCS和帧间隔，并输出64位数据加8位控制信号的XGMII接口。最后，PCS/PMA IP完成64b/66b编码、扰码和串行化，通过光口发送。

接收方向则执行相反的64位到8位转换，使原有802.1AS解析模块可以继续使用。这样既复用了成熟的千兆协议逻辑，又实现了在10G物理链路上的gPTP测试。需要说明的是，它适合低带宽的802.1AS报文测试，并不代表前端能够持续产生满线速10G业务流量。











#### 帧缓存设计

我使用的是64位DDR3物理总线，DDR时钟500MHz。因为DDR双沿传输，所以物理数据速率是1000MT/s，理论带宽是8GB/s。MIG采用4:1的PHY与控制器时钟比，因此`ui_clk`是125MHz，对应的原生用户数据宽度最高可以达到512位。MIG的AXI接口允许配置为32到512位，我最终选择128位，因此AXI用户侧理论带宽是125MHz乘128位，也就是2GB/s。选择的原因首先是这个位宽下的速率能够cover住同步帧的速率，同时与PCIe DMA的数据位宽相同，方便进行集成。

为了方便设计，我给每个帧固定分配的存储大小为128*8，也就是128byte，多余的地方补零，原因是由于位宽已经确定为128bit，考虑到研究所被测的最长帧长sync_follow报文长度为94byte，再加上16byte的帧头为110byte，理论上可以取7的深度但是为了在AXI突发的时候不会出现跨4K边界的情况，所以取最小的2的幂次作为深度，也就是8，AXI写参数固定为`AWLEN=7`、`AWSIZE=4`和INCR突发。

每条记录的第一拍是16 Byte元数据头，保存SFD时间戳、记录序号、端口号、TX/RX方向、PTP消息类型和实际帧长；后面保存帧数据，剩余位置补零。

选中一帧后，模块生成固定8拍记录，并写入跨时钟异步FIFO。只有FIFO中存在完整8拍记录时，才向DDR调度器发出请求，从而避免AXI突发过程中出现断帧。

四个端口之间采用帧粒度的Round-Robin仲裁。选中一个端口后锁存选择结果，连续写完该端口的一条128 Byte记录，中途不会切换端口。数据只在`WVALID`和`WREADY`同时有效时出队；如果MIG反压，当前数据、FIFO读指针和写拍计数器都保持不变。第8拍产生`WLAST`，收到写响应后，该端口的DDR地址增加128 Byte，再重新进行仲裁。

四个端口使用独立的DDR分区，每个端口分配512 MB，因此系统保证的是每个端口内部TX/RX的时间顺序，不需要在硬件中进行四端口全局排序。

测试期间系统只向DDR写入。测试结束、确认不再产生新帧后，再从DDR读取记录并通过128 bit PCIe DMA上传。这样读写阶段完全分离，能够进一步降低DDR控制和仲裁的复杂度。

整体上，我通过固定128 Byte记录统一了帧边界、AXI突发长度、DDR地址步长和DMA传输粒度；通过单端口时间排序和四端口Round-Robin仲裁，分别保证了端口内部顺序和多端口访问公平性。

单端口一共有五个FIFO，分别是TX/RX数据FIFO、TX/RX描述符FIFO和一个跨到DDR时钟域的输出FIFO。深度设计主要考虑最坏单帧占用和瞬时积压，而不是平均带宽。  

实际测试中最长的是携带32 Byte TLV的Follow_Up，保存长度为94 Byte，在128 bit接口上占6项。考虑一个完整帧因TX/RX时间排序而等待，同时下一帧正在接收，每个数据FIFO至少需要12项，向上取2的幂配置为16项。描述符每帧一个，结合16项数据FIFO最多容纳的短帧数量，描述符FIFO取4项。  

输出异步FIFO中每帧固定占8项。考虑四端口轮询时最差要等待其他三个端口，以及DDR短时反压，按4条完整记录配置为32项。该项目是低流量的802.1AS协议一致性测试，不承担线速压力流量，所以没有必要配置几十帧的缓存。

要想实现万兆：需要通过 `xgmii_analysis_rx` 和 `xgmii_analysis_tx` 两个模块识别收发方向的 gPTP 数据帧，并产生时间戳触发脉冲。由于 10G XGMII 接口每个时钟并行传输 8 字节，模块会逐个扫描 8 个 lane，通过控制字符 `0xFB` 和 `0xFD` 判断帧的开始与结束，同时统计帧内有效字节。根据以太网帧格式，在固定位置检查 EtherType 是否为 `0x88F7`，然后读取 PTP 报文的 `messageType`，区分 Sync、Pdelay Request、Pdelay Response、FollowUp 和 Announce 等报文，并为对应类型产生一个时钟周期的有效脉冲。该设计支持 XGMII 帧从 lane0 或 lane4 开始的情况，生成的脉冲再通过 `xpm_cdc_pulse` 从 156.25 MHz 的 XGMII 时钟域安全同步到 125 MHz 的业务时钟域，供下游锁存系统时间或触发响应报文。整体上，它采用了“帧起始检测、并行字节计数、协议字段识别、报文类型译码、脉冲跨时钟域”的设计思路。

# AXI ICN的设计与实现

### AXI Crossbar设计

在ICN中，最关键的是中间的AXI-Crossbar，在时序方面，我把寄存切片主要放在组合路由之后。前向 AW、W、AR 在 S 侧默认 bypass，在 M 侧分别使用 simple、skid、simple buffer；返回 B、R 在 M 侧 bypass，在 S 侧分别使用 simple 和 skid buffer。W、R 属于连续数据通道，因此使用无气泡的 skid buffer；AW、AR 和 B 的带宽压力相对较低，使用面积较小的 simple buffer。

对于写通路，每个输入端口首先通过 `axi_crossbar_addr` 完成地址译码，事务校验和安全权限判断，并得到目标输出端口和 Region。模块内部使用 thread 表跟踪活跃 ID。每个 thread 记录 ID、目的端口、Region 和该 ID 的在途事务数量。同一 ID 的新请求只有在目的端口和 Region 与已有记录一致时才能继续，否则会被阻塞，从而保证同 ID 事务的返回顺序。并发控制分为三个维度：`S_THREADS` 限制输入端口活跃的不同 ID 数量，`S_ACCEPT` 限制输入端口的总在途事务数，`M_ISSUE` 限制每个输出端口的总在途事务数。合法 AW 译码后，会向目标 M 端口发起轮询仲裁。仲裁选择的源输入端口会被锁定给 W 通道，直到对应 burst 的 `WLAST` 握手。因为 AXI4 的 W 通道没有 WID，所以必须通过 AW 建立并保持整个 W burst 的路由关系。AWID 发往下游时，在高位加入源输入端口索引。下游返回 B 时，根据 BID 高位恢复源输入端口，低位 ID 返回给上游。每个输入端口会在所有下游 B 响应与本地 DECERR 之间进行仲裁。如果 AW 地址译码失败，crossbar 不向任何下游端口发送 AW/W，而是拉高 WREADY 吸收完整写数据 burst，等 `WLAST` 完成后生成一拍 `BRESP=DECERR`。这样不会提前返回写响应。

读通路与写通路的地址译码、AR 仲裁和 ID 扩展基本一致，但没有 W 路由锁定。R 返回时根据 RID 高位路由到源输入端口，并在最后一拍 `RLAST` 完成时释放事务计数。对于读 DECERR，crossbar 根据 `ARLEN` 本地产生 `ARLEN+1` 拍零数据，所有拍的 `RRESP` 都为 DECERR，最后一拍拉高 RLAST。

在这个 ICN 项目中我还设计了 APB 配置接口和 AXI-to-AHB 桥接模块。

#### 你如何验证这个crossbar的正确性？

我采用“定向测试加约束随机测试”的方式验证 AXI Crossbar，利用ai agent搭建了一个自检查的 4×4 AXI VIP 验证环境。输入侧用4个 Master VIP产生事务，输出侧用4个 Slave VIP和memory model接收事务，同时通过参考地址译码、自检查任务和协议检查器，对路由结果、返回ID、响应码和读写数据进行自动比对。

首先验证基本路由。我让每个Master分别访问每个Slave的地址空间，覆盖完整的4×4路由矩阵，检查AW、AR是否只到达目标Slave，B、R响应是否返回原Master。

然后验证突发传输，包括FIXED、INCR、WRAP，不同burst长度、窄传输和部分WSTRB，并检查地址递增、WLAST/RLAST、读写数据以及响应是否正确。对于未映射地址、非法burst和跨4KB边界事务，检查Crossbar是否在输入侧返回DECERR，同时保证事务不会泄漏到下游Slave。写事务还要保证接收并丢弃到WLAST之后，才能返回错误响应。

并发方面，我让多个Master同时访问同一个Slave，验证round-robin仲裁和写数据路由锁定；还会发送多笔不同ID的Outstanding事务，并控制下游以不同顺序返回响应，验证Crossbar能根据扩展ID把响应恢复并路由给正确的Master。

最后，我在AW、W、B、AR、R五个通道的上下游随机插入backpressure，配合不同随机种子进行压力测试。功能覆盖率主要包括Master与Slave路由交叉、读写方向、burst类型、burst长度、映射与非映射地址、响应类型以及各通道stall。回归结束要求所有自检查用例通过、没有协议错误，并根据覆盖率空洞补充定向测试。

### APB接口

APB 从设备接口。一次访问分为 setup 和 access 两个阶段：setup 阶段由主机拉高 `PSEL` 并给出地址、读写方向和写数据；下一拍拉高 `PENABLE` 进入 access 阶段，模块通过 `PSEL&&PENABLE` 判断有效访问，本地的pready持续拉高。当前寄存器访问不需要插入等待周期，因此 `PREADY` 始终拉高。写操作根据 `PWRITE` 和地址译码，在握手时更新对应配置寄存器；读操作通过地址选择器将状态寄存器送到 `PRDATA`。对于未映射地址或读写属性错误的访问，通过 `PSLVERR` 返回错误。在此基础上，实现了端口使能、读写权限配置、错误状态捕获和请求统计等功能。

### AHB桥接

这个模块主要用于将 AXI4 接口转换为 AHB-Lite 接口，由于 AXI 的读写独立，而 AHB-Lite 同一时间只能顺序执行一个事务，因此我在 AXI 入口为 AW、AR 和 W 分别设置了 FIFO，用于吸收上游请求和缓解 AHB 侧反压；

#### fifo深度

当前 AW 和 AR 请求 FIFO 默认深度为 2，因为地址 entry 比较小，而且 AHB 后端只有一个串行 executor；当一笔事务正在执行时，再缓存两笔后续请求，已经能够吸收短期的 AXI 请求突发，继续增加深度不会提高 AHB 的实际执行带宽，只会延后上游被反压的时间。

W FIFO需要单独考虑，因为一笔 AW 对应最多 `AWLEN+1` 拍数据，AXI4 最大是 256 拍。为了在 AHB 执行当前事务时，能够缓存 AW FIFO 中两笔后续写事务的数据，我将 W FIFO 默认配置为 `2×MAX_BURST_LEN`，即 512 拍。这样请求 FIFO 的排队能力和写数据 FIFO 基本匹配，同时通过 WREADY 在 FIFO 满时正常向上游反压。

#### AHB桥接

采用burst级轮询，首次竞争写优先，对于写事务首先将写事务的len，size等信息锁存，并进行事务合法性校验，若不通过进入丢弃数据状态，将本burst的w数据全部读出之后跳转到响应产生状态，返回DECERR响应。当事务合法，会进入数据接收状态，将W通道的数据读出存入data buf中，掩码信号存入strb buf中，同时在读取的时候进行判断，若有任何一个beat不是全字节有效会将拆分标识置位。数据锁存完之后，若拆分标识未置位，跳转至非拆分事务发送状态，本状态首拍至HRANS为NONSEQ，再hready拉高的情况下，下一拍将写数据放在总线上，同时记录m_ahb_hresp，此后由hready来推进写事务直到发送的事务数与AXI burst len相同，跳转到响应返回阶段，只要途中有事务的hresp为高，就返回SLVERR。若拆分标识置位，就会跳转至拆分事务发送状态，将事务拆分为单个字节的AHB写事务，传输size固定为1字节，Hrans为NONSEQ，状态机初始跳转至拆分地址发送状态发送地址phase信号，hready拉高后跳转至拆分数据发送状态完成一笔事务的发送之后再跳转至拆分地址发送状态。拆分状态下会遍历strb信号，只取有效的lane进行发送，同时管理好地址，发送完成之后同样跳转至响应产生状态。（这样设计的主要原因是由于AXI的写可以支持beat的掩码操作，但是AHB不支持，所以将这种情况与狭窄传输一起被归为需要拆分的场景，拆分为多次NONSEQ的传输，方便设计）。

对于读事务不会像写事务一样分为两种情况，因为AXI读事务没有掩码，靠主机自身提取有效lane数据。读同样先给出首拍地址，在hready拉高的情况下，下一拍应该会出现读数据，将读数据与读响应分别存入rdata_buf与rresp_buf中，传输完成之后跳转到响应产生状态，将数据与响应返回R通道，直到传输长度与AXI burst len相同，跳转回IDLE状态完成事务。

### RNI节点设计

ICN中还可以挂载CHI的RNI节点，支持读写事务到CHI ReadOnce与WriteUniquePtl的转换。（注：ReadOnce的opcode为，WriteUniquePtl的opcode为）

从功能上看，该RNI是一个AXI到CHI的桥接模块；从CHI系统架构看，它也是一个RN-I协议端点，负责代表AXI侧I/O设备参与CHI事务和一致性访问。其整体架构如图所示，主要分为三个部分：读事务转换、写事务转换以及链路层控制。

#### 首先明确该设计位宽的选择：

这套RNI面向的是44位物理地址、64B cache line的系统。首先，AXI和CHI两侧物理地址宽度都配置成44位，避免协议转换过程中进行地址截断；CHI的SnpAddr配置成41位，是因为Snoop地址字段不携带最低3位，所以对应44位物理地址。

数据通路方面，AXI配置为128bit，也就是每拍16B。我把内部读写buffer的一个bank也设计成16B，这样正常满位宽AXI传输时，一拍数据直接落入一个bank，不需要额外的宽度转换。一个cache line是64B，因此内部自然划分成4个16B bank。

CHI DAT配置为256bit，也就是每个DAT flit携带32B，正好对应两个内部bank。因此一个完整64B cache line可以通过两个CHI DAT flit发送。相比CHI 128bit，它减少了一半数据flit数量；相比512bit，又能降低总线宽度和buffer读端口的面积压力，是吞吐与实现复杂度之间的折中。

CHI的Byte Enable配置为32bit，是因为一个CHI DAT flit有32B数据，每个bit对应一个byte。AXI侧多个WSTRB会先在16B bank中聚合，发送CHI DAT时再将两个bank的字节有效位组合成32bit BE。

beat count每个bank配置为4bit，也是由16B bank推导出来的。AXI最窄可以按1B传输，因此一个16B bank最多包含16个AXI beat。设计采用实际beat数减一编码，范围是0到15，刚好需要4bit。一个cache line有4个bank，所以整个beat-count vector是16bit。

并发方面，读写各配置32个entry。每个entry跟踪一个拆分后的CHI segment，包括AXI ID、目标地址、数据接收状态和CHI响应状态，因此它决定了RNI可以同时跟踪多少个未完成读写segment。32个entry需要5bit索引，能够提供一定的延迟隐藏能力，同时避免entry表、选择器和buffer面积继续线性增长。这个数不是协议规定的唯一值，而是当前目标配置的并发与面积折中。

最后，NID宽度配置成11bit，是为了与系统CHI网络的节点编号宽度一致；HNF NID和RNI NID分别是具体拓扑中的节点编号。Poison、DataCheck和RSVDC这些可选字段当前配置成0，是因为这个版本主要验证AXI到CHI基本事务转换，没有实现端到端数据校验和用户自定义扩展，可以减小flit和控制逻辑宽度。

#### 节点详细设计：

AXI 的一笔 burst 可能跨越多个64B cache line，也可能因为WRAP产生地址跳变；由于CHI协议只支持64B的cache line，为了设计更方便，所以将AXI事务拆分成可以由CHI entry独立管理的cacheline事务。因此我先按照64B cache line切分，保证每个segment只属于一条line；对于WRAP，即使不跨line，只要存在地址回绕，也会按高地址段和低地址段拆开，从而让后级始终处理连续地址。  

每个64B segment内部再划分成4个16B bank。选择16B是因为RNI当前的AXI数据宽度为128bit，一拍正好16B，而CHI DAT宽度为256bit，一拍正好携带两个bank，所以16B是AXI beat、CHI DAT和64B cache line之间最自然的公共管理粒度。通过CT、PD、LS、RV四组bank mask以及每个bank的beat count，我可以跟踪当前处理位置、待返回数据、最后一拍位置，并把CHI返回数据重新恢复成AXI beat。这样既避免把每个16B都转换成独立CHI请求，又能精确支持部分cache line和窄传输。

有了上面的参数，下面开始介绍RNI节点的详细设计：

首先介绍读事务转换。AXI总线上的AR控制信号会在AXI_BUS中打包，并通过握手写入AR FIFO。随后，segburst逐个处理FIFO中的AR请求，将其拆分为一个或多个不跨越64B Cacheline边界的CHI segment；只有当前Burst的所有segment都被下游接收后，才会弹出该FIFO项并开始处理下一笔AR事务。由于本设计面向128bit AXI、256bit CHI DAT和64B Cacheline的固定配置，因此选择三者的最大公共数据粒度16B作为bank大小。每个segment会包含地址以及三个Mask，其中分别用于标识起始bank，该segment覆盖的bank，原始AXI Burst最后一个Beat所在的bank。这些拆分的segment会被存放在下游的entry中，在存放的过程中会产生两条依赖链，分别是约束同ARID且同Cacheline的TXREQ发送的req chain以及约束同ARID事务返回AXI RDATA时不乱序的rdata chain。Entry准备完成后，ARCTRL会从满足发送条件的entry中仲裁一个用于组装TXREQ。候选entry按照QoS等级以及New/Retry状态分为四组，每组内部采用轮询仲裁，最终再按照“高QoS Retry、高QoS New、低QoS Retry、低QoS New”的固定优先级选出一个entry。其中，New候选是Req chain已经解除、尚未被选中发送且没有进入Retry流程的有效entry；Retry候选则是此前收到RetryAck，并且已经获得匹配PCrdGrant、具备重新发送资格的entry。选举出要发送请求的entry后进行REQ flit的组装，主要填写qos，srcid，tgtid等，其中opcode固定为readonce，TxnID采用本设计内部的编码方式，最高位固定为0表示读事务，低位填写entry 的 index，读size固定为64B，产生req flit后与链路层进行握手发送。在发送完readonce请求后，HN会返回2个dat flit（64/32），flit会进入rd buffer中，提取出data与resperr并根据flit中的txnid与dataid将数据存放在对应entry的对应bank中。数据返回后进行输出仲裁，Entry不需要等所有bank全部收齐，只要当前CTMASK指向的下一个待返回bank已经收到，并且该entry没有受到RDATA chain限制，就可以参与AXI RDATA输出仲裁。仲裁结束得到需要返回AXI RDATA的entry,bank索引，ARid等，将数据以及相应从相应bank中读出送入rpfifo中，并根据AXI size判断单个数据需要输出的个数，将数据再写入rdfifo中，只有最后一个展开Beat保留RLAST，rrd fifo与AXI R通道进行握手完成数据返回。

写请求与读请求在拆分segment时类似，区别在于产生的依赖链不同，AW entry分配时会建立三条依赖链，其基本匹配条件均为AWID相同：Req chain用于约束同AWID TXREQ的发送顺序,释放条件为接收到DBID；CompAck chain用于约束RNI向HN发送CompAck的顺序,释放条件为发送完毕compack；BResp chain用于保证同AWID事务向AXI返回BResp时不会乱序,释放条件为返回bresp。与此同时，拆分的segment会送入wr buffer将各个W Beat写入对应entry的对应16B bank，同时保存WSTRB信息。AWCTRL采用与读通路相同的四组仲裁选出entry，组装WriteUniquePtl请求，其中TxnID最高位置1表示写事务、低位为entry index。HN返回DBIDResp或CompDBIDResp后，Req chain解除；当entry已经获得DBID且W数据收齐后，即可独立参与TXDAT仲裁，wrbuffer将两个bank组装成一个256bit DAT Flit，并将WSTRB转换为CHI BE。数据发送完成并收到Comp后，若请求设置了ExpCompAck，则在CompAck chain解除后向HN发送CompAck。最后，当Comp、TXDAT和CompAck条件均满足且BResp chain解除后，将该segment的完成信息写入BResp FIFO；非末尾segment自动出队，只有原始AXI Burst最后一个segment产生BVALID/BRESP并与AXI B通道完成握手。

最后是链路层控制，主要负责LinkActive激活与退出握手，以及TXREQ、TXRSP、TXDAT、RXRSP和RXDAT五个CHI通道的Flit收发与L-Credit管理。

#### 关于链路激活的设计：

其中，TX通道记录HN授予RNI的信用，只有链路处于RUN且信用可用时才能发送Flit，发送后信用计数减一；RX通道在链路进入RUN后向HN发布初始信用，每接收一个Flit后再补回一个信用。此外，链路层还负责仲裁读写通路的TXREQ、在信用不足时保持待发送Flit，以及过滤接收到的信用返回Flit。链路进入DEACTIVATE后停止普通LCRDV交换，TX通道通过LCRDRETURN Flit归还未使用信用，RX通道则接收并统计HN返回的信用；只有信用全部收回后，链路才允许进入STOP状态。

#### 关于依赖链设计：



#### 关于TXNID的设计：







# NVME OVER TCP 硬卸载关键技术研究

项目背景：NVMe原本主要用于Host通过PCIe访问本地NVMe SSD。为了让服务器通过以太网访问远端NVMe存储，NVMe-oF定义了网络化的NVMe传输方式，其中NVMe/TCP使用TCP/IP承载NVMe命令、数据和完成信息，NVMe/TCP的传输如下所示：

```
Host
 │
 │ NVMe Command
 ▼
NVMe/TCP
 │
TCP
 │
IP
 │
Ethernet
 │
============= 网络 =============
 │
Ethernet
 │
IP
 │
TCP
 │
NVMe/TCP
 ▼
远端 NVMe Controller / NVM Subsystem
 │
 ▼
Storage
```

NVMe/TCP 并没有把 NVMe 的 Queue Model 删掉，而是把它映射到 TCP Connection。

官方 NVMe/TCP 规范规定：

> **一个 NVMe/TCP Connection 对应一个 Submission Queue / Completion Queue pair，如下图所示。**

```
Host                              Controller

SQ0 / CQ0
   │
   └──── TCP Connection 0 ────────┐
                                  │
SQ1 / CQ1                         │
   │                              │
   └──── TCP Connection 1 ────────┤ NVMe
                                  │ Subsystem
SQ2 / CQ2                         │
   │                              │
   └──── TCP Connection 2 ────────┘
```

NVMe/TCP 不再靠 PCIe TLP 搬这些信息，而是定义了自己的 **PDU（Protocol Data Unit）**。

| PDU         | 方向  | 用途                          |
| ----------- | ----- | ----------------------------- |
| ICReq       | H → C | 初始化 NVMe/TCP Connection    |
| ICResp      | C → H | 初始化响应                    |
| CapsuleCmd  | H → C | 携带 NVMe Command             |
| CapsuleResp | C → H | 携带 Completion               |
| H2CData     | H → C | Host 向 Controller 发送数据   |
| C2HData     | C → H | Controller 向 Host 发送数据   |
| R2T         | C → H | Controller 要求 Host 发送数据 |
| H2CTermReq  | H → C | Host 终止连接                 |
| C2HTermReq  | C → H | Controller 终止连接           |

交互流程如下：

NVMe/TCP Write 流程

```
Host                              Controller

 CapsuleCmd (Write)
─────────────────────────────────>

              R2T
<─────────────────────────────────

 H2CData
─────────────────────────────────>

 H2CData
─────────────────────────────────>

              CapsuleResp
<─────────────────────────────────
```

NVMe/TCP Read流程

```
Host                              Controller

 CapsuleCmd(Read)
─────────────────────────────────>

              C2HData
<─────────────────────────────────

              C2HData
<─────────────────────────────────

              CapsuleResp
<─────────────────────────────────
```

正式传 NVMe Command 之前，要先建立连接

```
Host                              Controller

        TCP SYN
─────────────────────────────────>

        SYN + ACK
<─────────────────────────────────

        ACK
─────────────────────────────────>

             TCP建立
                ↓

        ICReq
─────────────────────────────────>

        ICResp
<─────────────────────────────────>

       NVMe-oF Connect
─────────────────────────────────>

       Connect Response
<─────────────────────────────────>

             ↓
       Queue 建立完成
             ↓

       开始 NVMe I/O
```

本项目是中兴通讯领军班的结课微型课题。上游TCP协议栈维护8条逻辑连接，并通过共享的512-bit接口输入数据。本模块负责对各连接的完整PDU进行独立缓存和Round-Robin公平调度，将数据聚合限速至40 Gb/s后送入Parser。Parser把原始PDU及结构化解析结果交给下游NVMe控制器后端；对于ICReq或错误PDU，则生成响应并按照原连接号返回对应TCP通道。

项目背景：

> **Controller侧通过一个共享的网络接收前端承载8条TCP逻辑连接，本模块对这8条连接进行缓存、调度、限速和NVMe/TCP解析，随后把结果交给一个共享的NVMe控制器后端。**

从数据入口看，TCP数据通过`wr_data、wr_keep、wr_last和wr_conn`进入异步缓存模块。缓存按照连接号划分为8个逻辑区域，每条连接分别维护工作写指针、提交写指针和读指针。工作写指针随数据写入递增，只有收到`wr_last`后，才会更新提交写指针，表示一个完整PDU已经写入。提交写指针经过格雷码编码和跨时钟同步后进入核心时钟域，并与读指针比较产生`pending`信号，以此实现Doorbell报文就绪通知。读侧完成消费后更新读指针，并将其同步回写时钟域用于缓存满判断。工作指针与提交指针分离，可以避免Reader读取到尚未写完的PDU。

`pending[7:0]`送入轮询调度器。调度器根据Round-Robin策略，从存在完整PDU的连接中选择一路，并通过`grant_valid和grant_id`通知Reader。Reader空闲时拉高`grant_ready`完成授权握手，调度器同时更新优先级；Reader读取当前PDU期间不再接受新授权，直到末拍处理完成，从而保证不同连接的PDU不会交叉，同时实现连接之间的公平调度。

`nvmetcp_pdu_reader`根据授权连接读取缓存数据。它从首拍中取得`PLEN`，计算整个PDU对应的512-bit拍数，并在末拍检查`rd_last`和`rd_keep`是否与实际长度一致。数据通路采用ready/valid握手机制，下游发生背压时，Reader会保持当前数据及控制信号不变。

Reader与Parser之间设置了PDU级令牌桶限速模块。512-bit通路在200 MHz下的理论峰值为102.4 Gb/s，而令牌桶按照每周期25 Byte的速率补充令牌，对应40 Gb/s聚合带宽。令牌桶容量为1024 Byte，与最大PDU长度一致。模块在首拍读取`PLEN`，只有令牌足以覆盖整个PDU时才一次性扣除并放行，因此PDU内部不会因为限速而中断，限速主要体现在不同PDU之间的等待时间。

随后数据进入`nvmetcp_parser`。Parser解析首拍中的Type、Flags、HLEN、PDO和PLEN，支持ICReq、ICResp、H2CTermReq、C2HTermReq、CapsuleCmd、CapsuleResp、H2CData、C2HData和R2T共9类PDU。针对不同类型，模块还会提取CCCID、TTAG、数据偏移和数据长度等关键字段，并完成头部长度、PDU长度、Flags及字段对齐检查。

Parser具有两类主要输出：`payload_*`负责将完整原始PDU转发给外部命令或数据处理模块，`action_meta`负责输出连接号、PDU类型、关键字段、处理动作和错误信息等结构化结果。对于合法ICReq，Response Generator生成ICResp；如果发现协议或帧格式错误，则生成相应方向的TermReq。响应中的连接号来自原始PDU，因此能够通过`tx_conn`返回对应TCP连接；CapsuleCmd、H2CData等正常业务PDU则交给下游NVMe控制器后端继续处理。

## NVMe相关知识：

**NVMe Controller 和 NAND Flash 之间通过什么接口交互**，常见就是 **NAND Flash Interface**，工业上主要有 **ONFI** 和 **Toggle DDR** 两大类接口标准。

## PDU格式一览图：





# 你觉得项目的难点是什么，你是怎么解决这个难点？

## 1. IEEE 802.1AS 时间同步 IP 与硬件测试系统

> 我认为这个项目最大的难点，不是把 gPTP 报文收发出来，而是保证从硬件时间戳采集、链路时延计算、BMCA 选主到本地时钟校正的整个同步闭环具有确定性。
>
> 首先，PHY/GMII 收发时钟和系统时钟使用的 125 MHz 时钟并不完全同域。如果直接把多位时间戳和单周期有效信号跨时钟域传输，可能出现数据撕裂、亚稳态或者脉冲丢失，而时间戳只要偏几个周期，最终同步精度就会明显下降。其次，在 two-step 模式下，Sync 和 Follow_Up、Pdelay_Resp 和 Pdelay_Resp_Follow_Up 必须正确配对；扩展到四端口 Boundary Clock 后，还要保证最佳主时钟变化时，各端口角色能够一致切换。
>
> 我的解决方法是把时间戳尽量放在靠近 GMII 收发接口的位置，在报文固定位置产生硬件时间戳事件，减少后级解析流水线带来的不确定延迟。跨时钟域时，单周期事件使用脉冲同步器，保证事件和时间信息稳定到达协议时钟域。
>
> 协议处理上，我把 Sync、Pdelay、Follow_Up、Announce 分开解析，通过 sequence ID 和端口身份进行关联。等 T1、T2、T3、T4 等时间点全部有效后，状态机才启动链路时延和 offset 计算，然后把 offset、方向和 valid 送给时间管理模块修正本地 `sys_time`。
>
> 对四端口 BMCA，我先对各端口 Announce 信息做快照和超时老化，再按照 Priority1、clockClass、clockAccuracy、variance、Priority2、clockIdentity、stepsRemoved 逐级比较。为了避免宽组合比较影响时序，我把它拆成了多级串行比较。选出最佳端口后，由端口管理模块统一把该端口设置为 Slave，其余端口设置为 Master。
>
> 验证时，我先在仿真中注入已知链路时延和时钟偏差，检查 T1 到 T4、offset 和端口状态；板级再通过 ILA 和同步脉冲观察收敛过程。最终实现了 OC 和四端口 BC 的完整同步闭环，网内同步精度控制在 100 ns 以内。



我认为这个项目最大的难点，是如何在FPGA上构建一套真正可验证的802.1AS硬件测试闭环。它既要保证时间同步精度，又要能够产生各种正常和异常协议场景，同时还要保证多端口抓取的数据能够无丢失地缓存和上传。

这个问题实际上涉及三个层面。

第一，802.1AS协议本身对时间确定性要求很高。时间戳如果放在协议解析之后生成，就会受到流水线、仲裁和跨时钟域延迟的影响。因此我把时间戳采集放在GMII收发路径的固定字节位置，在报文真正进入或离开MAC附近完成硬件打点。多位时间戳通过异步FIFO跨时钟域，事件脉冲通过专门的CDC电路同步，避免直接打拍造成数据撕裂。

协议计算上，我将Sync、Follow_Up、Pdelay和Announce分别解析，通过sequenceId和有效标志完成报文配对；等T1到T4以及CorrectionField都有效后，再计算链路延迟和时钟偏差。BMCA部分则把本地时钟和各端口收到的Announce数据集统一送入比较模块，按照Priority1、clockClass、clockAccuracy、variance、Priority2、clockIdentity和stepsRemoved逐级比较，并结合Announce老化机制完成端口角色切换。这样在主时钟退出后，旧信息超时失效，BC可以重新选举新的从端口。

第二个难点是测试仪不能只会发送几个固定报文，它必须能够构造可重复的异常场景，并且保留足够的证据来判断结果。我在TestCenter中把每个端口设计成三种工作模式：协议端系统模式、测试及故障注入模式，以及探针转发模式。测试参数采用表项驱动，可以控制发送时间、报文类型和需要修改的字段，也可以制造Sync、Follow_Up、Pdelay、Announce丢失、超时、sequenceId错误以及BMCA优先级变化等场景。

探针模式可以对gPTP报文指定字段进行修改，同时透传普通以太网报文。模式切换只允许发生在帧边界，避免一帧报文的前半部分和后半部分走不同路径。接收侧同时保留两类信息：一类是解析后的结构化测试结果，另一类是原始报文数据。这样测试失败时，不只是得到一个错误码，还能上传原始报文进行追溯。

第三个难点是多端口连续抓包时，主机读取抖动或者PCIe反压不能影响前面的实时时序。我使用DDR作为协议测试和主机软件之间的弹性缓冲，每个端口设置独立FIFO、地址空间和写指针。报文按固定128字节记录存储，仲裁器选中一个端口后，会锁定该端口直到完成8个128-bit AXI beat，并且只有在`valid && ready`握手时才推进数据，从而避免DDR反压期间出现端口串包、丢拍或者WLAST错误。

PCIe部分我实现了参数化的多通道SG-DMA。难点主要是PCIe读完成报文可能乱序返回，并且一次传输还会受到MPS、MRRS以及4KB边界限制。我的处理方式是给读请求分配tag，通过reorder queue按tag保存Completion，再按照原始请求顺序和通道归属输出；同时在请求生成阶段计算距离4KB边界的剩余空间，对描述符自动拆分。各通道拥有独立状态机和缓冲，通过统一仲裁器复用PCIe请求接口，并把AXI-Stream反压一直传递到上游。

最后，我采用分层验证：先验证时间戳、报文解析、BMCA和DDR握手等单模块，再进行多端口系统仿真；测试仪侧覆盖字段错误、报文丢失、超时恢复和BMCA切换；DMA侧则专门构造跨通道、乱序Completion和随机反压。板级再通过ILA、1PPS以及反向同步方式观测。最终实现了简历中所写的亚100ns同步精度，并保证测试数据能够通过DDR和PCIe持续上传。

**面试官追问时重点展开：**

- 为什么时间戳要靠近 MAC/GMII，而不能在解析完成后再采集？
- 多位时间戳和单比特事件信号分别如何跨时钟域？
- Pdelay 的 T1～T4 如何计算链路时延？
- BMCA 发生主时钟切换时，如何避免使用过期 Announce？
- `<100 ns` 是如何测量的，包含哪些误差来源？

------

## 2. AMBA AXI Interconnect

**可直接口述：**

> 我认为这个项目最大的难点不是做地址译码和数据多路选择，而是在多主多从、Multiple Outstanding 和乱序返回的情况下，仍然保证 AXI 五个独立通道的路由关系和顺序语义正确。
>
> AXI 的 AW、W、B、AR、R 五个通道彼此独立，尤其 AXI4 的 W 通道没有 WID。如果只根据当前地址进行组合路由，AW 完成后仲裁结果可能变化，后续 W 数据就可能被送到错误的从端。另外，不同 ID 的响应允许乱序返回，但相同 ID 必须保持顺序；再叠加 backpressure、译码错误和 AHB-Lite 桥接，控制关系很容易出错。
>
> 我的解决方法首先是在请求进入 Crossbar 时，把源端口编号扩展到 AXI ID 的高位。比如内部 ID 宽度设计成原始 ID 宽度加上 `clog2(主端口数)`。这样下游即使不同 ID 乱序返回，也可以直接从 RID 或 BID 的高位恢复源端口，再把低位的原始 ID 返回给上游。
>
> 对写通道，我在 AW 握手后锁存目标端口和仲裁结果，并一直保持到对应的 WLAST 握手完成，保证整个 W burst 都跟随原来的 AW 路径。每个输出端口使用轮询仲裁，并通过 transaction counter 和 thread counter 分别限制目标端 Outstanding 数量，以及维护相同 ID 的顺序约束。
>
> 错误路径也需要遵守协议。例如写请求地址译码失败或 burst 跨越 4 KB 时，不能立即返回 B 响应，而是要继续吸收这笔事务的 W 数据直到 WLAST，然后返回 DECERR；读请求错误时，则要按照 ARLEN 返回完整的 DECERR R burst，并在最后一拍产生 RLAST，避免上游死锁。
>
> 对 AXI2AHB-Lite Bridge，我明确把 AHB 端设计成有序、单执行器模型，通过 AW、AR、W FIFO 吸收 AXI 前端请求，再串行转换成 AHB 访问，同时记录完整 ID，在响应时恢复。也就是说，直接 AXI 端口保留不同 ID 乱序能力，而 AHB 端口按照协议能力有序完成。
>
> 最后我用自检 testbench 和 AXI VIP，重点覆盖了多笔 Outstanding、不同 ID 反序返回、相同 ID 顺序约束、随机 backpressure、4 KB 边界、译码错误以及读写竞争等场景。

**面试官追问时重点展开：**

- AXI4 没有 WID，怎样保证 W 和 AW 的对应关系？
- 为什么把源端口号拼到 ID 中就能支持乱序返回？
- 相同 ID 为什么不能乱序，不同 ID为什么可以？
- 写地址非法时，为什么还要把 W 通道 drain 到 WLAST？
- AXI 到 AHB-Lite 转换后，哪些 AXI 能力会被主动降级？
- CHI RN-I 部分可继续展开 entry 管理、TxnID/DataID、RetryAck/PCRDGrant 和 ReadOnce/WriteUniquePtl 转换。

------

## 3. NVMe over TCP 硬件卸载模块

**可直接口述：**

> 我认为这个项目最大的难点，是要同时解决八连接跨时钟域缓存、公平调度、PDU 完整性和 40 Gb/s 精确限速，并且不能因为限速在一个 PDU 内部产生大量气泡。
>
> 数据通路是 512 bit、核心时钟目标是 200 MHz，物理峰值带宽高于 40 Gb/s，所以不能只依赖下游偶尔拉低 ready 来限速。另外，如果普通 Token Bucket 按每个 beat 判断令牌，可能在一个 PDU 传输到一半时令牌不足，造成 PDU 内停顿。八个连接又处于独立写入和核心处理时钟域，如果调度器看到的是尚未写完的 PDU，还可能读到半包。
>
> 我的第一个处理是为每个连接设计独立的异步环形缓存。写入侧分别维护工作指针和提交指针，数据写入过程中只推进工作指针，只有收到 wr_last、确认完整 PDU 到达后，才更新提交指针。提交指针和读取指针使用 Gray 编码跨时钟域。因此核心侧看到 pending 时，这个 PDU 一定已经完整写入，不会调度到半包。
>
> 第二个处理是采用 PDU 粒度的轮询调度。轮询仲裁器在一次 grant 握手后更新 mask，而 Reader 一旦选中某个连接，就持续读取到该 PDU 的 last，再释放给下一个连接。这样既保证了多连接长期公平，也避免不同连接的数据在一个 PDU 内交叉。
>
> 第三个处理是整包预留的 Token Bucket。系统每周期补充 25 Byte，200 MHz 下对应长期平均 40 Gb/s。读取 PDU 首拍时先解析 PLEN，只有桶内令牌不少于整个 PDU 长度时，才允许这个 PDU 开始发送；一旦开始，就进入 PASS 状态，整包连续通过，而不是每拍重新等待令牌。对于非法 PLEN，则退化成按每拍有效字节数计量。64 bit keep 的有效字节统计使用平衡加法树，减少组合路径。
>
> 整条链路统一采用 ready/valid 反压，Parser 再检查 PDU 类型、PLEN、最后一拍 keep 和 last 的一致性。验证中覆盖了八连接持续满负载、异步指针回绕、调度公平性、PDU 内无停顿以及异常长度等场景，RTL 回归测得有效吞吐约为 40 Gb/s，PDU 内最大间隔为一个周期。

# 项目中优化时序的方法(Performance)：

对于多级流水线设计,使用skidbuffer级联,减少了ready回传的路径长度,极大优化了时序。

在DMA设计中，状态机多采用独热码编码，使用assign is_s1 = state[1]这种方式判断当前状态，缩短了组合逻辑链路长度。

在802.1AS的计算的设计中,使用右移的方式来实现除2的功能,降低组合逻辑链优化时序,在BMCA算法中,使用流水拆分大位宽的向量比较,优化时序.

在涉及到优先级编码器的设计中，使用二叉树的形式进行设计。

我曾在vivado上做过一个小实验，对于8bit的输入数据寻找最低位LSB，分别采用casez的优先级编码器以及二叉树的实现方式，接入同一个时序逻辑中进行综合，使用SDC约束不断提高时钟频率，发现最后在时钟周期为1.04ns的情况下，casez的优先级编码方式发生了违例，二叉树的实现却没有。



在AXI-Crossbar的设计中，根据各个通道的流量特征，我没有统一使用同一种寄存器切片。地址通道 AW 和 AR 每个 burst 只传输一次，但译码和仲裁路径较长，因此使用 simple buffer 切断关键路径；W 和 R 是连续 beat 的高吞吐数据通道，为了在反压下避免 bubble，使用 skid buffer；B 响应通道频率较低，使用 simple buffer 即可。其他路径优先 bypass，避免额外面积和延迟。最终是根据 STA 结果，在面积、频率、吞吐率和 latency 之间做权衡。



# 项目中优化面积的方法(Area)：

在 RTL 编写时，我会根据每个寄存器或中间信号的实际取值范围来定义位宽,如根据最大取值范围使用 `$clog2` 参数化计算寄存器位宽。

在 PHY 侧跨时钟域设计中，恢复时钟和系统时钟都是 125MHz，上下游数据位宽相同，所以平均吞吐率是相同的。从理论估算看，较小深度就可以满足，例如 6 左右；但考虑到 Gray 指针设计通常要求深度取 2 的幂，同时为了留一定的裕量，最终将 FIFO 深度设置为 16。

在802.1AS的计算的设计中,使用右移的方式来实现除2的功能.优化面积。

在PCIe DMA的乱序返回的RAM设计中,我可以根据MRRS计算出每个Tag可以返回的最大数据量,对fifo深度进行最小裕量设置。

对于描述符的深度设置,可以根据驱动侧参数进行合理配置,确保能够完整存储一次描述符的最大值







# 项目中优化功耗的方法(Power)：











# AI提问

**1. 你先怎么整体介绍这个项目？**
“这个项目是一个基于 PCIe Gen2 x8 的多通道 SG-DMA 引擎，目标是在 FPGA 和主机内存之间实现高带宽、低 CPU 参与度的数据搬运。我主要负责 TLP 处理引擎、completion 乱序重排、基于 credit 的流控、多通道 DMA 通路搭建，以及驱动和 RTL 的联调闭环。”

**2. 为什么要做 SG-DMA，而不是普通连续 DMA？**
“因为主机用户态 buffer 在物理上通常是不连续的，不能假设是一整段连续物理地址。驱动需要先 pin 页并生成 SG 表，FPGA 再按 SG 描述符分段访问主机内存。这样既能支持大块数据，也能兼容真实操作系统里的离散内存布局。”

**3. 这个架构你是怎么分层的？**
“我把它分成三层。最左边是 PCIe 协议接入层，负责 TLP 收发和配置空间对接；中间是公共基础设施，包括 BAR 寄存器、中断、reorder queue、credit flow control 和发送仲裁；右边是按 channel 复制的数据通路，每个 channel 分成上报和下发两条链路。”

**4. TX/RX 处理引擎是怎么做全流水解析的？**
“接收侧先由 rx_engine 把 TLP + valid + SOP/EOP + offset 全部打一条统一延迟链，然后分别送给 rxr_engine 和 rxc_engine 去解析 request 和 completion。发送侧则是 txr_engine 和 txc_engine 分别负责 request 和 completion 的组包，最后统一进入发送仲裁。这样把协议解析拆成了前端对齐和后端专用解析两部分，更利于 250MHz 下时序收敛。”

**5. 为什么 RX 要拆成 rxr_engine 和 rxc_engine？**
“因为 request 和 completion 的格式差异很大。request 这边要区分 3DW/4DW、MRd/MWr，还要做地址重组；completion 这边更关注 tag、byte count、lower address 和 payload 对齐。分开做以后，控制逻辑更清晰，时序压力也更小。”

**6. 为什么需要 reorder queue？**
“因为多个 MRd 发出去之后，主机返回的 completion 可能乱序到达，不能直接按到达顺序送给后级。我这里是先按 tag 把数据写入分段 RAM，再由输出状态机按 tag 顺序扫描并恢复成连续的 128bit 数据流，这样后级看到的就是逻辑上有序的数据。”

**7. 为什么 reorder queue 用 4 个 32bit RAM bank？**
“因为当前 PCIe 用户侧宽度是 128bit，也就是 4 个 DW。拆成 4 个 32bit bank 后，同一地址并行读取就能自然拼成一个 128bit beat，这样既方便按 DW 粒度做 shift，也方便和后面的 packet_128、FIFO 这类模块对齐。”

**8. reorder_queue_output 为什么要对 4DW、8DW 单独处理？**
“因为当前输出粒度就是 128bit = 4DW/拍，所以 4DW 和 8DW 分别对应单拍包和双拍包边界。对短包做特判可以减少状态切换，缩短尾拍处理路径；更长的包再进入通用 streaming 路径，这样结构更清晰，控制也更稳。”

**9. 你这个乱序输出状态机怎么讲？**
“我一般分四个状态讲。S0 是等待并扫描完成 tag；S2 是首拍输出并判断总长度；如果总长度大于 8DW，就进 S3 做中间拍流式输出；最后进 S1 做尾拍输出和 tag clear。它的本质是把已经乱序收回来的 completion，重新按 tag 顺序、按 128bit 拍吐给后级。”

**10. 发送仲裁是怎么做的？**
“发送仲裁在 tx_mux_arbiter 里做，当前是按包粒度实现 WRR。它不是按 beat 切换，而是只有在一个 TLP 发完，也就是 EOP 时，才允许在 TXR 和 TXC 之间切换。这样既保证 request 吞吐优先，也不会把 completion 长时间饿死。”

**12. 12 个通道你是怎么支持的？**
“顶层是参数化多通道架构，emoki 里通过 generate 方式按 C_NUM_CHNL 实例化多个 emoki_channel，每个 channel 都有独立的 TX/RX 事务状态、SG 处理和用户侧接口；中间的 reorder queue、interrupt、tx_multiplexer 这些是共享资源。也就是说，我做的是‘通道私有状态 + 公共仲裁资源’的结构。”

**13. 12 通道验证这件事如果面试官追问，你怎么答？**
“我会说我不仅是参数上支持，而是在顶层实际完成了 12 通道例化，并验证了多通道下的基本收发链路、中断上报和共享资源访问。验证重点是检查多通道同时存在时，reorder、共享发送路径和中断向量不会互相串扰。”

**14. 中断模块你怎么讲？**
“中断模块把每个 channel 的 5 类关键事件打包成向量，包括 TX 事务开始、TX SG 收到、TX 完成、RX SG 收到、RX 完成。12 个通道一共 60 bit，分两组 32bit vector 上报给主机。这样驱动通过读向量寄存器就能知道哪个通道发生了哪类事件。”

**15. 为什么中断向量要分两组 32bit？**
“因为 12 个通道、每通道 5 类事件，总共是 60 个 bit，一组 32bit 放不下，所以拆成两组 vector。这样结构简单，也方便驱动按位解析。”

**16. Credit flow control 是怎么设计的？**
“我做的是保守型 credit 流控。每发一个 MRd，就先预留最坏情况下接收该 completion 所需的 header/data 资源；等这笔 completion 被完整处理后，再归还 credit。这样能避免接收缓冲区被打满，在复杂并发下更稳。”

**17. 为什么你的流控是保守型，而不是精确型？**
“因为精确型虽然理论并发更高，但控制复杂度更大，而且更容易在边界情况下出错。保守型设计用 MRRS、RCB 和 completion credit 上限估最坏占用，牺牲一点极限吞吐，换更稳的系统行为，我觉得在工程上是更合理的取舍。”

**18. 上报链路怎么讲？**
“上报是 FPGA 先发出事务通知，主机驱动收到后准备接收 buffer 的 SG 表，FPGA 再先把 SG 表读回来，再根据 SG 描述符把用户数据写回主机内存。这条链路的核心模块是 tx_port_channel_gate、tx_port_monitor、tx_port_writer、tx_port_buffer 和共享发送路径。”

**19. 下发链路怎么讲？**
“下发是驱动先准备发送 buffer 的 SG 表，FPGA 先把 SG 表读回来，再按 SG 描述符发 MRd 去主机取真正的数据，completion 回来后先进入 reorder queue，重排之后再送到对应用户通道。核心在 rx_port、sg_list_req、reorder_queue 和 rx_port_channel_gate。”

**20. tx_port_buffer 为什么需要？**
“因为用户数据是连续流，而 tx_port_writer 切出来的每个写请求长度不一定是 4DW 对齐。tx_port_buffer 的作用就是把前一个请求尾部剩下的非 4DW 数据和下一个请求的数据重新拼起来，确保下游每次看到的都是当前 PCIe 写请求应该发出的 128bit 数据。”

**21. 这套 DMA 是 byte 级的吗？**
“当前这版不是 byte-native DMA，而是以 32bit DW 为单位。驱动 API、SG 长度和 RTL 通道接口都是按 DW 定义的，所以我会把它定义成 DW 粒度的 SG-DMA 引擎。”

**22. 83% 带宽利用率你怎么解释？**
“Gen2 x8 理论原始带宽大约 4GB/s，83% 对应有效 payload 吞吐大约 3.3GB/s。考虑到 8b/10b 编码、TLP header、SG 读取、completion 重排和驱动交互，这个结果是比较合理的。它说明系统不是只停留在功能正确，而是已经做到了比较高的链路利用率。”

**23. 你这个项目最难的点是什么？**
“我觉得最难的不是写某一个状态机，而是把协议层、数据路径、乱序重排、流控和驱动协同真正串起来。尤其是多通道下，问题不再是单点功能，而是共享资源之间会不会互相影响，这部分我花了很多精力去梳理和验证。”

**24. 如果让你总结这个项目的工程价值，你怎么说？**
“我会说它体现的是系统级 RTL 设计能力。不是单个模块做对了就结束，而是要把 TLP、DMA、SG、驱动、中断、流控和多通道扩展放到同一个系统里考虑，最后还能在 250MHz 下收敛并跑出较高带宽。”

**25. 最后一问：你在这个项目里最能体现个人能力的部分是什么？**
“我最想强调的是两点。第一，我能把复杂协议问题拆成清晰的模块边界，比如解析、重排、仲裁、流控和通道私有逻辑。第二，我不是只会写 RTL，而是能把驱动和硬件一起看，把整条 DMA 交互流程真正跑通。”







# NVMe/TCP 九类 PDU 格式与 RTL 解析映射

## ICReq：Initialize Connection Request

### 功能

Host在TCP连接建立后发送ICReq，向Controller提出建立NVMe/TCP连接，并声明数据对齐、Digest和R2T能力。

### 标准格式

| 字节偏移 |     位宽 | 字段     | 含义                                                         |
| -------: | -------: | -------- | ------------------------------------------------------------ |
|      `0` |    8 bit | PDU-Type | 固定为`00h`                                                  |
|      `1` |    8 bit | FLAGS    | 保留，应为0                                                  |
|      `2` |    8 bit | HLEN     | 固定为128，即`80h`                                           |
|      `3` |    8 bit | PDO      | 保留，无DATA，应为0                                          |
|    `4~7` |   32 bit | PLEN     | 固定为128                                                    |
|    `8~9` |   16 bit | PFV      | PDU Format Version，1.0d中为0                                |
|     `10` |    8 bit | HPDA     | Host要求Controller发送DATA时采用的数据对齐参数，0基、单位Dword |
|     `11` |    8 bit | DGST     | bit 0为HDGST_ENABLE，bit 1为DDGST_ENABLE                     |
|  `12~15` |   32 bit | MAXR2T   | 单个命令允许存在的最大未完成R2T数量，0基编码                 |
| `16~127` | 112 Byte | Reserved | 保留，应为0                                                  |

### 当前RTL处理

- 检查`HLEN=128`、`PLEN=128`和`PDO=0`。
- 检查`PFV=0`。
- 当前设计要求`HPDA=0`、`DGST=0`，不支持非默认对齐和Digest。
- 输出`ACT_ICREQ`动作，由Response Generator生成ICResp。
- 当前`action_meta`不保存HPDA、DGST和MAXR2T。

## ICResp：Initialize Connection Response

### 功能

Controller接收合法ICReq后发送ICResp，确认连接初始化，并返回Controller侧的数据对齐、Digest和H2CData长度能力。

### 标准格式

| 字节偏移 |     位宽 | 字段       | 含义                                                         |
| -------: | -------: | ---------- | ------------------------------------------------------------ |
|      `0` |    8 bit | PDU-Type   | 固定为`01h`                                                  |
|      `1` |    8 bit | FLAGS      | 保留，应为0                                                  |
|      `2` |    8 bit | HLEN       | 固定为128，即`80h`                                           |
|      `3` |    8 bit | PDO        | 保留，无DATA，应为0                                          |
|    `4~7` |   32 bit | PLEN       | 固定为128                                                    |
|    `8~9` |   16 bit | PFV        | PDU Format Version，1.0d中为0                                |
|     `10` |    8 bit | CPDA       | Controller要求Host发送DATA时采用的数据对齐参数，0基、单位Dword |
|     `11` |    8 bit | DGST       | bit 0为HDGST_ENABLE，bit 1为DDGST_ENABLE                     |
|  `12~15` |   32 bit | MAXH2CDATA | 单个H2CData PDU允许携带的最大DATA字节数                      |
| `16~127` | 112 Byte | Reserved   | 保留，应为0                                                  |

### 当前RTL处理

- 检查`HLEN=128`、`PLEN=128`和`PDO=0`。
- 检查`PFV=0`。
- 当前设计要求`CPDA=0`、`DGST=0`。
- 输出`ACT_ICRESP`，当前Response Generator不会再次产生回复。
- 当前`action_meta`不保存CPDA、DGST和MAXH2CDATA。

## H2CTermReq：Host to Controller Terminate Request

### 功能

Host检测到Controller方向的致命传输错误时发送H2CTermReq，请求终止当前NVMe/TCP连接。

### 标准格式

|    字节偏移 |         位宽 | 字段     | 含义                                            |
| ----------: | -----------: | -------- | ----------------------------------------------- |
|         `0` |        8 bit | PDU-Type | 固定为`02h`                                     |
|         `1` |        8 bit | FLAGS    | 保留，应为0                                     |
|         `2` |        8 bit | HLEN     | 固定为24，即`18h`                               |
|         `3` |        8 bit | PDO      | 保留，应为0                                     |
|       `4~7` |       32 bit | PLEN     | 24～152 Byte                                    |
|       `8~9` |       16 bit | FES      | Fatal Error Status，致命错误类型                |
|     `10~13` |       32 bit | FEI      | Fatal Error Information，错误字段偏移或附加信息 |
|     `14~23` |      10 Byte | Reserved | 保留，应为0                                     |
| `24~PLEN-1` | 最多128 Byte | DATA     | 发生错误时正在处理的PDU Header，可选            |

### 当前RTL处理

- 检查`HLEN=24`、`PDO=0`和`PLEN<=152`。
- 提取`FES`和`FEI`到`action_meta`。
- 输出`ACT_TERMINATE`，收到TermReq本身不再生成TermResp。

## C2HTermReq：Controller to Host Terminate Request

### 功能

Controller检测到Host方向的致命传输错误时发送C2HTermReq，请求Host终止当前NVMe/TCP连接。

### 标准格式

C2HTermReq与H2CTermReq字段布局相同，仅Opcode、方向和部分FES语义不同。

|    字节偏移 |         位宽 | 字段     | 含义                                 |
| ----------: | -----------: | -------- | ------------------------------------ |
|         `0` |        8 bit | PDU-Type | 固定为`03h`                          |
|         `1` |        8 bit | FLAGS    | 保留，应为0                          |
|         `2` |        8 bit | HLEN     | 固定为24，即`18h`                    |
|         `3` |        8 bit | PDO      | 保留，应为0                          |
|       `4~7` |       32 bit | PLEN     | 24～152 Byte                         |
|       `8~9` |       16 bit | FES      | Fatal Error Status                   |
|     `10~13` |       32 bit | FEI      | Fatal Error Information              |
|     `14~23` |      10 Byte | Reserved | 保留，应为0                          |
| `24~PLEN-1` | 最多128 Byte | DATA     | 发生错误时正在处理的PDU Header，可选 |

### 当前RTL处理

- 与H2CTermReq采用相同的格式检查和字段提取。
- 当前Controller侧设计检测到非法Host PDU时，也会通过Response Generator主动生成C2HTermReq。

## CapsuleCmd：Command Capsule

### 功能

Host使用CapsuleCmd向Controller发送一条NVMe-oF命令。PDU Header内部包含完整的64 Byte SQE，后面还可以携带可选的In-Capsule Data。

### 标准格式

|         字节偏移 |    位宽 | 字段     | 含义                                 |
| ---------------: | ------: | -------- | ------------------------------------ |
|              `0` |   8 bit | PDU-Type | 固定为`04h`                          |
|           `1[0]` |   1 bit | HDGSTF   | 为1表示Header后存在4 Byte HDGST      |
|           `1[1]` |   1 bit | DDGSTF   | 为1表示DATA后存在4 Byte DDGST        |
|         `1[7:2]` |   6 bit | Reserved | 保留，应为0                          |
|              `2` |   8 bit | HLEN     | 固定为72，即8 Byte CH加64 Byte SQE   |
|              `3` |   8 bit | PDO      | In-Capsule Data起始偏移；无DATA时为0 |
|            `4~7` |  32 bit | PLEN     | PDU总长度                            |
|           `8~71` | 64 Byte | CCSQE    | NVMe-oF Command Capsule SQE          |
|          `72~75` |  4 Byte | HDGST    | `HDGSTF=1`时存在                     |
| `HLEN(+4)~PDO-1` |    可变 | PAD      | 为满足CPDA插入的对齐填充             |
|        `PDO~M-1` |    可变 | CCICD    | 可选的NVMe-oF In-Capsule Data        |
|          `M~M+3` |  4 Byte | DDGST    | DATA存在且`DDGSTF=1`时存在           |

SQE自身的CID位于SQE Byte 2～3，因此相对整个PDU位于Byte 10～11：

```text
CCCID = PDU Byte 10~11
```

### 当前RTL处理

- 检查`HLEN=72`。
- `PLEN=72`时要求`PDO=0`，表示没有内联数据。
- `PLEN>72`时要求`PDO=72`，不支持HDGST和PAD导致的可变PDO。
- 从Byte 10～11提取`CCCID`。
- 内联数据长度计算为`PLEN-PDO`。
- 输出`ACT_CAPSULE_CMD`，完整SQE通过`payload_*`继续转发。

## CapsuleResp：Response Capsule

### 功能

Controller使用CapsuleResp向Host返回一条NVMe命令的完成结果。PDU Header中包含完整的16 Byte CQE。

### 标准格式

| 字节偏移 |    位宽 | 字段     | 含义                               |
| -------: | ------: | -------- | ---------------------------------- |
|      `0` |   8 bit | PDU-Type | 固定为`05h`                        |
|   `1[0]` |   1 bit | HDGSTF   | 为1表示Header后存在4 Byte HDGST    |
| `1[7:1]` |   7 bit | Reserved | 保留，应为0                        |
|      `2` |   8 bit | HLEN     | 固定为24，即8 Byte CH加16 Byte CQE |
|      `3` |   8 bit | PDO      | 保留，无DATA，应为0                |
|    `4~7` |  32 bit | PLEN     | 无HDGST时为24，有HDGST时为28       |
|   `8~23` | 16 Byte | RCCQE    | NVMe-oF Response Capsule CQE       |
|  `24~27` |  4 Byte | HDGST    | `HDGSTF=1`时存在                   |

CQE中的CID相对整个PDU位于Byte 20～21：

```text
CCCID = PDU Byte 20~21
```

### 当前RTL处理

- 检查`HLEN=24`、`PLEN=24`和`PDO=0`。
- 当前不支持HDGST形式的28 Byte CapsuleResp。
- 从Byte 20～21提取`CCCID`。
- 输出`ACT_CAPSULE_RESP`，完整CQE通过`payload_*`继续转发。

## H2CData：Host to Controller Data

### 功能

Host使用H2CData向Controller发送命令数据，典型场景是NVMe写操作。标准Command Data Buffer流程中，H2CData通常响应Controller之前发送的R2T。

### 标准格式

|         字节偏移 |       位宽 | 字段     | 含义                                      |
| ---------------: | ---------: | -------- | ----------------------------------------- |
|              `0` |      8 bit | PDU-Type | 固定为`06h`                               |
|           `1[0]` |      1 bit | HDGSTF   | Header Digest存在标志                     |
|           `1[1]` |      1 bit | DDGSTF   | Data Digest存在标志                       |
|           `1[2]` |      1 bit | LAST_PDU | 当前PDU是否为对应R2T的最后一个H2CData     |
|         `1[7:3]` |      5 bit | Reserved | 保留，应为0                               |
|              `2` |      8 bit | HLEN     | 固定为24                                  |
|              `3` |      8 bit | PDO      | DATA起始偏移，应满足CPDA对齐              |
|            `4~7` |     32 bit | PLEN     | PDU总长度                                 |
|            `8~9` |     16 bit | CCCID    | 关联CapsuleCmd中的SQE.CID                 |
|          `10~11` |     16 bit | TTAG     | 关联之前收到的R2T                         |
|          `12~15` |     32 bit | DATAO    | 当前DATA在Command Data Buffer中的起始偏移 |
|          `16~19` |     32 bit | DATAL    | 当前PDU的有效DATA长度                     |
|          `20~23` |     32 bit | Reserved | 保留，应为0                               |
|          `24~27` |     4 Byte | HDGST    | `HDGSTF=1`时存在                          |
| `HLEN(+4)~PDO-1` |       可变 | PAD      | 为满足CPDA插入的填充                      |
|        `PDO~M-1` | DATAL Byte | DATA     | 实际写数据                                |
|          `M~M+3` |     4 Byte | DDGST    | `DDGSTF=1`时存在                          |

`DATAO`和`DATAL`都必须满足4 Byte对齐。

### 当前RTL处理

- 检查`HLEN=24`和`PDO=24`。
- 检查`DATAL=PLEN-24`，因此当前不支持HDGST、PAD和DDGST。
- 检查`DATAO`和`DATAL`均为4 Byte对齐。
- 提取`CCCID`、`TTAG`、`DATAO`和`DATAL`。
- 输出`ACT_H2C_DATA`。

## C2HData：Controller to Host Data

### 功能

Controller使用C2HData向Host返回命令数据，典型场景是NVMe读操作。

### 标准格式

|         字节偏移 |       位宽 | 字段     | 含义                                          |
| ---------------: | ---------: | -------- | --------------------------------------------- |
|              `0` |      8 bit | PDU-Type | 固定为`07h`                                   |
|           `1[0]` |      1 bit | HDGSTF   | Header Digest存在标志                         |
|           `1[1]` |      1 bit | DDGSTF   | Data Digest存在标志                           |
|           `1[2]` |      1 bit | LAST_PDU | 是否为当前命令最后一个C2HData                 |
|           `1[3]` |      1 bit | SUCCESS  | 命令成功完成，且Controller不再发送CapsuleResp |
|         `1[7:4]` |      4 bit | Reserved | 保留，应为0                                   |
|              `2` |      8 bit | HLEN     | 固定为24                                      |
|              `3` |      8 bit | PDO      | DATA起始偏移，应满足HPDA对齐                  |
|            `4~7` |     32 bit | PLEN     | PDU总长度                                     |
|            `8~9` |     16 bit | CCCID    | 关联CapsuleCmd中的SQE.CID                     |
|          `10~11` |     16 bit | Reserved | 保留，应为0                                   |
|          `12~15` |     32 bit | DATAO    | 当前DATA在Host数据缓冲区中的偏移              |
|          `16~19` |     32 bit | DATAL    | 当前PDU的有效DATA长度                         |
|          `20~23` |     32 bit | Reserved | 保留，应为0                                   |
|          `24~27` |     4 Byte | HDGST    | `HDGSTF=1`时存在                              |
| `HLEN(+4)~PDO-1` |       可变 | PAD      | 为满足HPDA插入的填充                          |
|        `PDO~M-1` | DATAL Byte | DATA     | 实际读数据                                    |
|          `M~M+3` |     4 Byte | DDGST    | `DDGSTF=1`时存在                              |

`SUCCESS=1`通常应与`LAST_PDU=1`配合使用，表示Host可以根据当前PDU合成成功CQE，不再等待CapsuleResp。

### 当前RTL处理

- 检查`HLEN=24`和`PDO=24`。
- 检查`DATAL=PLEN-24`，不支持Digest和Padding。
- 检查`DATAO`和`DATAL`均为4 Byte对齐。
- 提取`CCCID`、`DATAO`和`DATAL`，C2HData没有TTAG字段。
- 原始Flags保存在`action_meta`中，因此后级可读取`LAST_PDU`和`SUCCESS`。
- 输出`ACT_C2H_DATA`。

## R2T：Ready to Transfer

### 功能

Controller发送R2T，通知Host可以为指定命令发送一段H2CData，并给出传输标签、偏移和长度。

### 标准格式

| 字节偏移 |   位宽 | 字段     | 含义                                      |
| -------: | -----: | -------- | ----------------------------------------- |
|      `0` |  8 bit | PDU-Type | 固定为`09h`                               |
|   `1[0]` |  1 bit | HDGSTF   | Header Digest存在标志                     |
| `1[7:1]` |  7 bit | Reserved | 保留，应为0                               |
|      `2` |  8 bit | HLEN     | 固定为24                                  |
|      `3` |  8 bit | PDO      | 保留，无DATA，应为0                       |
|    `4~7` | 32 bit | PLEN     | 无HDGST时为24，有HDGST时为28              |
|    `8~9` | 16 bit | CCCID    | 关联CapsuleCmd中的SQE.CID                 |
|  `10~11` | 16 bit | TTAG     | Controller产生的Transfer Tag              |
|  `12~15` | 32 bit | R2TO     | 请求数据相对Command Data Buffer的起始偏移 |
|  `16~19` | 32 bit | R2TL     | Controller允许Host发送的数据长度          |
|  `20~23` | 32 bit | Reserved | 保留，应为0                               |
|  `24~27` | 4 Byte | HDGST    | `HDGSTF=1`时存在                          |

`R2TO`和`R2TL`都必须满足4 Byte对齐。Host后续发送的H2CData通过CCCID和TTAG与该R2T关联。

### 当前RTL处理

- 检查`HLEN=24`、`PLEN=24`和`PDO=0`。
- 当前不支持带HDGST的28 Byte R2T。
- 检查`R2TO`和`R2TL`满足4 Byte对齐。
- 提取`CCCID`、`TTAG`、`R2TO`和`R2TL`。
- 在`action_meta`中将`R2TO`映射为`data_offset`，将`R2TL`映射为`data_length`。
- 输出`ACT_R2T`。



# 项目中遇到的bug，如何解决的？



# 综合问题

## 你觉得你的优缺点是什么？

优点：

缺点：







